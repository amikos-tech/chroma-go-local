---
phase: quick/260730-p2k
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
  - .github/workflows/release.yml
autonomous: true
requirements: [ISSUE-96]

must_haves:
  truths:
    - "No workflow in .github/workflows/ references arduino/setup-protoc"
    - "All 4 protoc setup steps use chroma-core/setup-protoc pinned to SHA df9e7872eaabfd0ddfafd9e27fe77c6229bc7d22"
    - "Each of the 4 steps still passes version: \"31.x\" and repo-token: ${{ secrets.GITHUB_TOKEN }} unchanged"
    - "ci.yml and release.yml remain valid YAML (yq parses both) and valid Actions syntax (actionlint clean)"
    - "No action pin other than setup-protoc is modified — the diff contains exactly 4 changed lines, all setup-protoc uses: lines"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "3 protoc setup steps on the chroma-core fork (build matrix jobs)"
      contains: "chroma-core/setup-protoc@df9e7872eaabfd0ddfafd9e27fe77c6229bc7d22"
    - path: ".github/workflows/release.yml"
      provides: "1 protoc setup step on the chroma-core fork (build job)"
      contains: "chroma-core/setup-protoc@df9e7872eaabfd0ddfafd9e27fe77c6229bc7d22"
  key_links:
    - from: "each replaced uses: line"
      to: "its existing sibling with: block"
      via: "indentation preserved at 8 spaces so version/repo-token stay bound to the same step"
      pattern: "^        uses: chroma-core/setup-protoc@df9e7872eaabfd0ddfafd9e27fe77c6229bc7d22 # v4c$"
    - from: "pinned SHA df9e7872"
      to: "chroma-core/setup-protoc default branch"
      via: "commit SHA pin (repo convention for third-party actions), verified resolvable via gh api"
      pattern: "setup-protoc@[0-9a-f]{40} #"
---

<objective>
Replace `arduino/setup-protoc` with the `chroma-core/setup-protoc` fork at all 4 call sites so protoc setup stops running on the deprecated Node 20 Actions runtime (GitHub issue #96).

Purpose: `arduino/setup-protoc` has had no release in ~2 years and its default branch still declares `runs.using: node20`, which GitHub is deprecating. The `chroma-core` fork is 4 commits ahead / 0 behind upstream, bumping the runtime to Node 24 and adding retry/backoff on the GitHub releases API call. Cross-platform support (darwin/linux, x86_64/aarch_64) is intact in the fork, matching this repo's ubuntu/macos/windows matrix.

Output: 4 modified `uses:` lines across 2 workflow files. Nothing else.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/STATE.md
@CLAUDE.md

This is a pure CI configuration change. No Go, Rust, or Java source is touched. No new tests are written — validation is YAML/Actions-syntax linting plus a diff-scope assertion.

**Current state (verified at plan time — all 4 sites are byte-identical):**

```yaml
      - name: Set up protoc
        uses: arduino/setup-protoc@c65c819552d16ad3c9b72d9dfd5ba5237b9c906b # v3.0.0
        with:
          version: "31.x"
          repo-token: ${{ secrets.GITHUB_TOKEN }}
```

Call sites (line numbers verified at plan time — note release.yml drifted from the 54 reported in the issue to 60):

| File | Line |
|------|------|
| `.github/workflows/ci.yml` | 54 |
| `.github/workflows/ci.yml` | 124 |
| `.github/workflows/ci.yml` | 198 |
| `.github/workflows/release.yml` | 60 |

**Target state — only the `uses:` line changes; the `with:` block is untouched:**

```yaml
      - name: Set up protoc
        uses: chroma-core/setup-protoc@df9e7872eaabfd0ddfafd9e27fe77c6229bc7d22 # v4c
        with:
          version: "31.x"
          repo-token: ${{ secrets.GITHUB_TOKEN }}
```

The `uses:` line is indented 8 spaces. Preserve that exactly.

**Tooling confirmed present:** `yq` v4.53.3 (`/opt/homebrew/bin/yq`), `actionlint` (`~/go/bin/actionlint`), `gh`.

**Pin verified at plan time:** `gh api repos/chroma-core/setup-protoc/commits/df9e7872eaabfd0ddfafd9e27fe77c6229bc7d22` resolves — commit message "upgrade to httpm to kill a warning".

**Editing note:** use the Edit tool for these replacements. Do not attempt `perl -i` / `sed -i` in-place rewrites on these files.
</context>

<tasks>

<task type="auto">
  <name>Task 1: Swap the setup-protoc action pin at all 4 call sites</name>
  <files>.github/workflows/ci.yml, .github/workflows/release.yml</files>
  <action>
Replace the line `uses: arduino/setup-protoc@c65c819552d16ad3c9b72d9dfd5ba5237b9c906b # v3.0.0` with `uses: chroma-core/setup-protoc@df9e7872eaabfd0ddfafd9e27fe77c6229bc7d22 # v4c` at ci.yml lines 54, 124, 198 and release.yml line 60.

Change ONLY the `uses:` line at each site. Do not touch the adjacent `- name: Set up protoc` line, the `with:` block, `version`, `repo-token`, or the following `Verify protoc` step. Do not modify any other action pin in these files (`actions/checkout`, `actions/setup-go`, `Swatinem/rust-cache`, `dtolnay/rust-toolchain`, cosign, etc.) — issue #96 is scoped to setup-protoc only.

Keep the existing 8-space indentation on the replaced line so `with:` stays bound to the same step. Keep the `# v4c` trailing comment: this repo pins third-party actions by SHA with a human-readable version comment, and `v4c` is the fork's tag for this commit.

All 4 occurrences are byte-identical, so in ci.yml use a replace-all Edit on the single literal `uses:` line rather than 3 positional edits.
  </action>
  <verify>
    <automated>test "$(grep -c 'uses: chroma-core/setup-protoc@df9e7872eaabfd0ddfafd9e27fe77c6229bc7d22 # v4c$' .github/workflows/ci.yml)" = "3" && test "$(grep -c 'uses: chroma-core/setup-protoc@df9e7872eaabfd0ddfafd9e27fe77c6229bc7d22 # v4c$' .github/workflows/release.yml)" = "1" && test "$(grep -rh 'setup-protoc' .github/workflows/ | grep -v '^[[:space:]]*#' | grep -c 'arduino/')" = "0" && echo OK</automated>
  </verify>
  <done>ci.yml contains exactly 3 and release.yml exactly 1 `chroma-core/setup-protoc@df9e7872...` pin; zero non-comment references to `arduino/setup-protoc` remain anywhere under .github/workflows/.</done>
</task>

<task type="auto">
  <name>Task 2: Validate YAML, Actions syntax, and diff scope</name>
  <files>.github/workflows/ci.yml, .github/workflows/release.yml</files>
  <action>
Validate the edits without changing any file.

1. Parse both workflows with `yq` per this repo's CLAUDE.md convention ("use yq for validating yaml files").
2. Use `yq` to structurally confirm all 4 setup-protoc steps resolved as real step objects with their inputs intact — this catches indentation damage that a plain grep would miss (e.g. a `with:` block orphaned from its step). The query shape below was dry-run at plan time against the pre-edit files and returned 4.
3. Run `actionlint` on both workflows to confirm GitHub Actions schema validity.
4. Assert diff scope: `git diff` over `.github/workflows/` must show exactly 4 added and 4 removed lines, and every one of those 8 changed lines must contain `setup-protoc`. If any other line changed, revert the stray edit before finishing.

If `actionlint` warns about the fork being an unknown/non-upstream action, that is expected metadata noise — only hard errors (schema/syntax) block completion.
  </action>
  <verify>
    <automated>yq eval '.' .github/workflows/ci.yml > /dev/null && yq eval '.' .github/workflows/release.yml > /dev/null && test "$(yq eval-all '[.jobs[].steps[]? | select((.uses // "") | contains("setup-protoc")) | select(.with.version == "31.x") | select(.with["repo-token"] != null) | select(.uses == "chroma-core/setup-protoc@df9e7872eaabfd0ddfafd9e27fe77c6229bc7d22")] | length' .github/workflows/ci.yml .github/workflows/release.yml | paste -sd+ - | bc)" = "4" && actionlint .github/workflows/ci.yml .github/workflows/release.yml && test "$(git diff -U0 -- .github/workflows/ | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | wc -l | tr -d ' ')" = "8" && test "$(git diff -U0 -- .github/workflows/ | grep -E '^[+-]' | grep -vE '^(\+\+\+|---)' | grep -c 'setup-protoc')" = "8" && echo OK</automated>
  </verify>
  <done>Both workflows parse under yq; yq resolves exactly 4 setup-protoc steps on the chroma-core pin each carrying `version: "31.x"` and a `repo-token`; actionlint exits 0; the git diff is exactly 8 changed lines and all 8 mention setup-protoc.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| GitHub Actions runner → third-party action | Action code executes with `secrets.GITHUB_TOKEN` in scope on CI and release runners |
| Release job → published artifacts | The release workflow builds and signs artifacts on the same runner where this action runs |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-p2k-01 | Tampering | `chroma-core/setup-protoc` action code executed in CI and release jobs | mitigate | Pin by full 40-char commit SHA `df9e7872...`, not a mutable tag or branch. SHA verified resolvable at plan time via `gh api repos/chroma-core/setup-protoc/commits/<sha>`. Task 1 verify greps for the exact SHA so a silent re-pin cannot pass. |
| T-p2k-02 | Elevation of Privilege | `repo-token: ${{ secrets.GITHUB_TOKEN }}` passed to the fork | accept | Same token exposure as the current `arduino/setup-protoc` usage — no new privilege is granted by this change. The fork is org-owned (`chroma-core`), 0 commits behind upstream, and its 4 delta commits are a runtime bump plus releases-API retry/backoff. Token is the default job-scoped `GITHUB_TOKEN`. |
| T-p2k-03 | Denial of Service | GitHub releases API rate limiting during protoc download | mitigate | The fork's retry/backoff hardening on the releases API call is one of the reasons for adopting it; no additional work needed in this plan. |
| T-p2k-SC | Tampering | Supply chain — action swap scope creep | mitigate | Task 2 asserts the git diff is exactly 8 changed lines and all 8 contain `setup-protoc`, so no other action pin can be altered in the same change. No npm/pip/cargo installs are introduced by this plan, so no package legitimacy gate applies. |
</threat_model>

<verification>
- `grep -rn 'setup-protoc' .github/workflows/` shows 4 lines, all `chroma-core/setup-protoc@df9e7872eaabfd0ddfafd9e27fe77c6229bc7d22 # v4c`
- `yq eval '.'` succeeds on both `ci.yml` and `release.yml`
- `actionlint .github/workflows/ci.yml .github/workflows/release.yml` exits 0
- `git diff --stat -- .github/workflows/` reports 2 files changed, 4 insertions(+), 4 deletions(-)
- No source, build, or test files appear in `git status`
</verification>

<success_criteria>
- All 4 `arduino/setup-protoc` references replaced with the SHA-pinned `chroma-core/setup-protoc` fork
- `version: "31.x"` and `repo-token: ${{ secrets.GITHUB_TOKEN }}` unchanged at every site
- Both workflow files valid under `yq` and `actionlint`
- Diff is exactly 4 lines changed (8 diff lines), all setup-protoc — no collateral pin edits
- GitHub issue #96 fully addressed: no Node 20 runtime dependency remains from setup-protoc
</success_criteria>

<output>
Create `.planning/quick/260730-p2k-replace-arduino-setup-protoc-with-chroma/260730-p2k-SUMMARY.md` when done
</output>
