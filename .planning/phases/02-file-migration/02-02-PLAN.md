---
phase: 02-file-migration
plan: 02
type: execute
wave: 2
depends_on: ["02-01"]
files_modified:
  - internal/runtime/chroma.go
  - internal/runtime/config.go
  - internal/runtime/embedded.go
  - internal/runtime/errors.go
  - internal/runtime/backup.go
  - internal/runtime/rebuild.go
  - internal/runtime/compaction.go
  - internal/runtime/wal_prune.go
  - internal/runtime/chroma_test.go
  - internal/runtime/embedded_test.go
  - internal/runtime/embedded_benchmark_test.go
  - internal/runtime/embedded_integration_edge_test.go
  - internal/runtime/embedded_metadata_validation_test.go
  - internal/runtime/embedded_property_test.go
  - internal/runtime/embedded_create_collection_persistence_test.go
  - internal/runtime/backup_test.go
  - internal/runtime/rebuild_test.go
  - internal/runtime/compaction_test.go
  - internal/runtime/wal_prune_test.go
  - internal/runtime/runtime.go
  - internal_test.go
autonomous: true
requirements: [LAYOUT-03]

must_haves:
  truths:
    - "All FFI globals (libHandle, libOnce, ffiMu, 40+ function pointers) exist exclusively in internal/runtime/chroma.go"
    - "The repo root contains zero Go implementation files (only go.mod, go.sum remain)"
    - "internal_test.go (Phase 1 anchor) is removed"
    - "go build ./... succeeds with no implementation at root"
    - "All test files exist in internal/runtime/ with package runtime declaration"
    - "stdlib runtime import aliased as goruntime in all internal/runtime files that use it"
    - "Relative paths in tests fixed (shim/Cargo.toml -> ../../shim/Cargo.toml)"
    - "go test -race -run ^$ ./internal/... compiles race-instrumented test binaries without data-race reports"
  artifacts:
    - path: "internal/runtime/chroma.go"
      provides: "FFI globals, Init(), Server type, registerFunctions, callFFI helpers"
      contains: "package runtime"
      exports: ["Init", "Server", "Version"]
    - path: "internal/runtime/config.go"
      provides: "ServerConfig, builder pattern, NewServer"
      contains: "package runtime"
    - path: "internal/runtime/embedded.go"
      provides: "Embedded type and all embedded methods"
      contains: "package runtime"
    - path: "internal/runtime/errors.go"
      provides: "Error codes, sentinel errors"
      contains: "package runtime"
    - path: "internal/runtime/backup.go"
      provides: "Backup methods on Server/Embedded"
      contains: "package runtime"
    - path: "internal/runtime/rebuild.go"
      provides: "Rebuild methods on Embedded"
      contains: "package runtime"
    - path: "internal/runtime/compaction.go"
      provides: "Compact methods on Embedded"
      contains: "package runtime"
    - path: "internal/runtime/wal_prune.go"
      provides: "WAL prune methods on Embedded"
      contains: "package runtime"
    - path: "internal/runtime/chroma_test.go"
      provides: "Core tests including TestInitAndVersion"
      contains: "package runtime"
  key_links:
    - from: "internal/runtime/chroma.go"
      to: "internal/library"
      via: "import in Init() -> library.LoadLibrary()"
      pattern: 'library\.LoadLibrary'
    - from: "internal/runtime/chroma.go"
      to: "stdlib runtime"
      via: "aliased import goruntime"
      pattern: 'goruntime "runtime"'
    - from: "internal/runtime/chroma_test.go"
      to: "../../shim/Cargo.toml"
      via: "relative path in readShimCargoVersion"
      pattern: '../../shim/Cargo.toml'
---

<objective>
Move all remaining Go implementation and test files from the repo root into internal/runtime/, alias stdlib runtime imports, fix relative test paths, and remove the Phase 1 anchor test. After this plan, the repo root contains zero Go implementation logic.

Purpose: Second half of the two-commit migration strategy. Completes the file migration phase by moving all FFI globals, Server/Embedded types, and tests into internal/runtime/. This satisfies LAYOUT-03 (FFI globals moved atomically, no split state).

Output: 19 files in internal/runtime/, Phase 1 skeleton and anchor test removed, zero implementation files at root.
</objective>

<execution_context>
@/Users/tazarov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-file-migration/02-CONTEXT.md
@.planning/phases/02-file-migration/02-RESEARCH.md
@.planning/phases/02-file-migration/02-01-SUMMARY.md

<interfaces>
<!-- After Plan 01, chroma.go at root already imports internal/library -->

From chroma.go (root -- current state after Plan 01):
```go
package chroma

import (
    "fmt"
    "runtime"
    "strings"
    "sync"
    "sync/atomic"
    "unsafe"

    "github.com/amikos-tech/chroma-go-local/internal/library"
    "github.com/ebitengine/purego"
    "github.com/pkg/errors"
)

var (
    libHandle uintptr
    libOnce   sync.Once
    libErr    error
    ffiMu     sync.Mutex
    // ... 40+ FFI function pointers ...
)

func Init(libPath string) error {
    libOnce.Do(func() {
        libHandle, libErr = library.LoadLibrary(libPath)
        // ...
    })
}
```

From internal/library/ (completed in Plan 01):
```go
package library
func LoadLibrary(path string) (uintptr, error)
```

From internal/runtime/runtime.go (Phase 1 skeleton -- will be REMOVED):
```go
// Package runtime contains the Chroma server and embedded runtime implementation.
package runtime
```

Files that import stdlib "runtime" and NEED goruntime alias (moving to package runtime):
- chroma.go: runtime.SetFinalizer, runtime.KeepAlive
- embedded.go: runtime.SetFinalizer, runtime.KeepAlive
- backup.go: runtime.KeepAlive
- rebuild.go: runtime.KeepAlive
- compaction.go: runtime.KeepAlive
- wal_prune.go: runtime.KeepAlive
- backup_test.go: runtime.GOOS

Files that do NOT import stdlib "runtime" (no alias needed):
- config.go, errors.go
- chroma_test.go, embedded_test.go, embedded_benchmark_test.go
- embedded_integration_edge_test.go, embedded_metadata_validation_test.go
- embedded_property_test.go, embedded_create_collection_persistence_test.go
- compaction_test.go, rebuild_test.go, wal_prune_test.go

Relative path fix needed:
- chroma_test.go line 88: os.ReadFile("shim/Cargo.toml") -> os.ReadFile("../../shim/Cargo.toml")
- chroma_test.go line 116: error message references "shim/Cargo.toml" -> "../../shim/Cargo.toml"
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Move implementation files to internal/runtime/ with package and import fixes</name>
  <files>
    internal/runtime/chroma.go
    internal/runtime/config.go
    internal/runtime/embedded.go
    internal/runtime/errors.go
    internal/runtime/backup.go
    internal/runtime/rebuild.go
    internal/runtime/compaction.go
    internal/runtime/wal_prune.go
    internal/runtime/runtime.go
  </files>
  <read_first>
    chroma.go
    config.go
    embedded.go
    errors.go
    backup.go
    rebuild.go
    compaction.go
    wal_prune.go
    internal/runtime/runtime.go
  </read_first>
  <action>
1. Read all 8 root implementation files and the Phase 1 skeleton `internal/runtime/runtime.go`.

2. Move `chroma.go` to `internal/runtime/chroma.go`:
   - Change `package chroma` to `package runtime`
   - Transfer the doc comment from Phase 1 skeleton: `// Package runtime contains the Chroma server and embedded runtime implementation.` -- place it above the package declaration
   - Change `"runtime"` import to `goruntime "runtime"` (stdlib alias to avoid collision with package name)
   - Replace all `runtime.SetFinalizer(` with `goruntime.SetFinalizer(`
   - Replace all `runtime.KeepAlive(` with `goruntime.KeepAlive(`
   - The `"github.com/amikos-tech/chroma-go-local/internal/library"` import stays as-is (already added in Plan 01)
   - `library.LoadLibrary(libPath)` call in Init() stays as-is
   - All FFI globals (libHandle, libOnce, libErr, ffiMu, 40+ function pointers) move with this file unchanged

3. Move `config.go` to `internal/runtime/config.go`:
   - Change `package chroma` to `package runtime`
   - This file does NOT import stdlib `"runtime"`, so no alias needed
   - No other import changes needed

4. Move `embedded.go` to `internal/runtime/embedded.go`:
   - Change `package chroma` to `package runtime`
   - Change `"runtime"` import to `goruntime "runtime"`
   - Replace all `runtime.SetFinalizer(` with `goruntime.SetFinalizer(`
   - Replace all `runtime.KeepAlive(` with `goruntime.KeepAlive(`

5. Move `errors.go` to `internal/runtime/errors.go`:
   - Change `package chroma` to `package runtime`
   - This file does NOT import stdlib `"runtime"`, so no alias needed

6. Move `backup.go` to `internal/runtime/backup.go`:
   - Change `package chroma` to `package runtime`
   - Change `"runtime"` import to `goruntime "runtime"`
   - Replace all `runtime.KeepAlive(` with `goruntime.KeepAlive(`

7. Move `rebuild.go` to `internal/runtime/rebuild.go`:
   - Change `package chroma` to `package runtime`
   - Change `"runtime"` import to `goruntime "runtime"`
   - Replace all `runtime.KeepAlive(` with `goruntime.KeepAlive(`

8. Move `compaction.go` to `internal/runtime/compaction.go`:
   - Change `package chroma` to `package runtime`
   - Change `"runtime"` import to `goruntime "runtime"`
   - Replace all `runtime.KeepAlive(` with `goruntime.KeepAlive(`

9. Move `wal_prune.go` to `internal/runtime/wal_prune.go`:
   - Change `package chroma` to `package runtime`
   - Change `"runtime"` import to `goruntime "runtime"`
   - Replace all `runtime.KeepAlive(` with `goruntime.KeepAlive(`

10. Delete the Phase 1 skeleton `internal/runtime/runtime.go` (its doc comment has been transferred to chroma.go).

11. Delete all 8 original files from the repo root:
    `rm chroma.go config.go embedded.go errors.go backup.go rebuild.go compaction.go wal_prune.go`
  </action>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/local-go-chroma && for f in chroma config embedded errors backup rebuild compaction wal_prune; do grep -c "^package runtime" "internal/runtime/${f}.go" || echo "FAIL: ${f}.go missing package runtime"; done && grep -c 'goruntime "runtime"' internal/runtime/chroma.go internal/runtime/embedded.go internal/runtime/backup.go internal/runtime/rebuild.go internal/runtime/compaction.go internal/runtime/wal_prune.go && test ! -f chroma.go && test ! -f config.go && test ! -f embedded.go && test ! -f errors.go && test ! -f backup.go && test ! -f rebuild.go && test ! -f compaction.go && test ! -f wal_prune.go && test ! -f internal/runtime/runtime.go && echo "TASK1_PASS"</automated>
  </verify>
  <acceptance_criteria>
    - All 8 files (`chroma.go`, `config.go`, `embedded.go`, `errors.go`, `backup.go`, `rebuild.go`, `compaction.go`, `wal_prune.go`) exist in `internal/runtime/` with `package runtime`
    - `internal/runtime/chroma.go` contains `goruntime "runtime"` (not bare `"runtime"`)
    - `internal/runtime/embedded.go` contains `goruntime "runtime"`
    - `internal/runtime/backup.go` contains `goruntime "runtime"`
    - `internal/runtime/rebuild.go` contains `goruntime "runtime"`
    - `internal/runtime/compaction.go` contains `goruntime "runtime"`
    - `internal/runtime/wal_prune.go` contains `goruntime "runtime"`
    - No `runtime.SetFinalizer` or `runtime.KeepAlive` calls remain (all replaced with `goruntime.` prefix)
    - `internal/runtime/runtime.go` (Phase 1 skeleton) no longer exists
    - None of the 8 original files exist at repo root
    - `internal/runtime/chroma.go` has the package doc comment: `// Package runtime contains the Chroma server and embedded runtime implementation.`
    - `internal/runtime/chroma.go` contains `library.LoadLibrary` (import bridge from Plan 01 preserved)
    - `grep -r "var libHandle" internal/runtime/chroma.go` finds the FFI globals
  </acceptance_criteria>
  <done>All 8 implementation files live in internal/runtime/ with correct package declarations, goruntime alias for stdlib runtime, and FFI globals intact. Phase 1 skeleton removed. No implementation files remain at root.</done>
</task>

<task type="auto">
  <name>Task 2: Move test files to internal/runtime/, fix relative paths, remove anchor test, verify build</name>
  <files>
    internal/runtime/chroma_test.go
    internal/runtime/embedded_test.go
    internal/runtime/embedded_benchmark_test.go
    internal/runtime/embedded_integration_edge_test.go
    internal/runtime/embedded_metadata_validation_test.go
    internal/runtime/embedded_property_test.go
    internal/runtime/embedded_create_collection_persistence_test.go
    internal/runtime/backup_test.go
    internal/runtime/rebuild_test.go
    internal/runtime/compaction_test.go
    internal/runtime/wal_prune_test.go
    internal_test.go
  </files>
  <read_first>
    chroma_test.go
    embedded_test.go
    embedded_benchmark_test.go
    embedded_integration_edge_test.go
    embedded_metadata_validation_test.go
    embedded_property_test.go
    embedded_create_collection_persistence_test.go
    backup_test.go
    rebuild_test.go
    compaction_test.go
    wal_prune_test.go
    internal_test.go
  </read_first>
  <action>
1. Read all 11 root test files and `internal_test.go`.

2. Move `chroma_test.go` to `internal/runtime/chroma_test.go`:
   - Change `package chroma` to `package runtime`
   - This file does NOT import stdlib `"runtime"`, so no alias needed
   - Fix relative path in `readShimCargoVersion` function: change `os.ReadFile("shim/Cargo.toml")` to `os.ReadFile("../../shim/Cargo.toml")` (two directories up from internal/runtime/ to repo root)
   - Fix the error message string on the t.Fatal line that references `shim/Cargo.toml` to `../../shim/Cargo.toml`

3. Move `embedded_test.go` to `internal/runtime/embedded_test.go`:
   - Change `package chroma` to `package runtime`
   - No stdlib `"runtime"` import, no alias needed

4. Move `embedded_benchmark_test.go` to `internal/runtime/embedded_benchmark_test.go`:
   - Change `package chroma` to `package runtime`

5. Move `embedded_integration_edge_test.go` to `internal/runtime/embedded_integration_edge_test.go`:
   - Change `package chroma` to `package runtime`

6. Move `embedded_metadata_validation_test.go` to `internal/runtime/embedded_metadata_validation_test.go`:
   - Change `package chroma` to `package runtime`
   - This file directly assigns to FFI function pointers (`chromaEmbeddedUpdateCollection`, `chromaEmbeddedCreateCollection`) for mocking -- this works because tests are white-box `package runtime`

7. Move `embedded_property_test.go` to `internal/runtime/embedded_property_test.go`:
   - Change `package chroma` to `package runtime`

8. Move `embedded_create_collection_persistence_test.go` to `internal/runtime/embedded_create_collection_persistence_test.go`:
   - Change `package chroma` to `package runtime`

9. Move `backup_test.go` to `internal/runtime/backup_test.go`:
   - Change `package chroma` to `package runtime`
   - This file DOES import stdlib `"runtime"` -- change to `goruntime "runtime"` and replace `runtime.GOOS` with `goruntime.GOOS`

10. Move `rebuild_test.go` to `internal/runtime/rebuild_test.go`:
    - Change `package chroma` to `package runtime`
    - Check if it imports stdlib `"runtime"`. If yes, alias to `goruntime "runtime"` and replace usages.

11. Move `compaction_test.go` to `internal/runtime/compaction_test.go`:
    - Change `package chroma` to `package runtime`
    - Check if it imports stdlib `"runtime"`. If yes, alias to `goruntime "runtime"` and replace usages.

12. Move `wal_prune_test.go` to `internal/runtime/wal_prune_test.go`:
    - Change `package chroma` to `package runtime`
    - Check if it imports stdlib `"runtime"`. If yes, alias to `goruntime "runtime"` and replace usages.

13. Delete `internal_test.go` (Phase 1 anchor test -- no longer needed, real code in internal/ is a stronger proof).

14. Delete all 11 original test files from the repo root:
    `rm chroma_test.go embedded_test.go embedded_benchmark_test.go embedded_integration_edge_test.go embedded_metadata_validation_test.go embedded_property_test.go embedded_create_collection_persistence_test.go backup_test.go rebuild_test.go compaction_test.go wal_prune_test.go`

15. Verify the full build:
    - `go build ./...` must succeed (no Go implementation files at root)
    - `go vet ./internal/...` must succeed
    - `go test -race -run ^$ ./internal/...` must succeed (compiles race-instrumented test binaries with no test execution -- verifies no compile-time race issues; `-run ^$` matches zero tests so no shim is required)
    - Verify no .go implementation files remain at root: `ls *.go` should show only `go.mod` and `go.sum` (no .go files at all)
  </action>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/local-go-chroma && for f in chroma_test embedded_test embedded_benchmark_test embedded_integration_edge_test embedded_metadata_validation_test embedded_property_test embedded_create_collection_persistence_test backup_test rebuild_test compaction_test wal_prune_test; do grep -c "^package runtime" "internal/runtime/${f}.go" || echo "FAIL: ${f}.go"; done && grep '../../shim/Cargo.toml' internal/runtime/chroma_test.go && test ! -f internal_test.go && test ! -f chroma_test.go && test ! -f embedded_test.go && test ! -f backup_test.go && ls *.go 2>/dev/null | wc -l | tr -d ' ' | grep '^0$' && go build ./... && go vet ./internal/... && go test -race -run ^$ ./internal/... && echo "TASK2_PASS"</automated>
  </verify>
  <acceptance_criteria>
    - All 11 test files exist in `internal/runtime/` with `package runtime` declaration
    - `internal/runtime/chroma_test.go` contains `../../shim/Cargo.toml` (not `shim/Cargo.toml`)
    - `internal/runtime/backup_test.go` contains `goruntime "runtime"` (not bare `"runtime"`)
    - `internal_test.go` no longer exists at repo root
    - Zero `.go` files exist at the repo root (`ls *.go 2>/dev/null` returns nothing)
    - `go build ./...` exits 0
    - `go vet ./internal/...` exits 0
    - `go test -race -run ^$ ./internal/...` exits 0 (race-instrumented compilation clean)
    - `internal/runtime/embedded_metadata_validation_test.go` contains `package runtime` (white-box access to FFI function pointers)
    - No test file at root (`chroma_test.go`, `embedded_test.go`, etc. all deleted)
  </acceptance_criteria>
  <done>All 11 test files live in internal/runtime/ with correct package declarations and relative path fixes. Phase 1 anchor test removed. Zero .go files remain at repo root. go build, go vet, and go test -race compile check all pass.</done>
</task>

</tasks>

<verification>
After both tasks:
- `go build ./...` exits 0 (compilation with no implementation at root)
- `go vet ./internal/...` exits 0
- `go test -race -run ^$ ./internal/...` exits 0 (race-instrumented test compilation clean -- per phase goal)
- `ls *.go 2>/dev/null` returns nothing (zero Go files at root)
- `grep -r "var libHandle" internal/runtime/chroma.go` confirms FFI globals in runtime
- `grep -rL "^package runtime" internal/runtime/*.go` returns nothing (all files have package runtime)
- `GOOS=linux go build ./internal/...` exits 0
- `GOOS=windows go build ./internal/...` exits 0
- No `runtime.SetFinalizer` or `runtime.KeepAlive` without `goruntime` prefix in internal/runtime/
</verification>

<success_criteria>
- All FFI globals (libHandle, libOnce, ffiMu, 40+ function pointers) exist exclusively in internal/runtime/chroma.go -- zero var state at root (LAYOUT-03)
- All 8 implementation files and 11 test files in internal/runtime/ with package runtime
- stdlib runtime aliased as goruntime in all internal/runtime files that need it
- Relative path to shim/Cargo.toml fixed to ../../shim/Cargo.toml in chroma_test.go
- internal_test.go removed
- Zero .go files at repo root
- go build ./... and go vet ./internal/... pass
- go test -race -run ^$ ./internal/... passes (race-instrumented compilation gate from phase goal)
- Cross-compile with GOOS=linux and GOOS=windows passes for ./internal/...
</success_criteria>

<output>
After completion, create `.planning/phases/02-file-migration/02-02-SUMMARY.md`
</output>
