---
phase: 06-core-foundation-types
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - java/core/build.gradle.kts
  - java/core/src/main/java/tech/amikos/chroma/local/core/JsonUtil.java
  - java/core/src/main/java/tech/amikos/chroma/local/core/RebuildCollectionResult.java
  - java/core/src/main/java/tech/amikos/chroma/local/core/CompactionCollectionResult.java
  - java/core/src/main/java/tech/amikos/chroma/local/core/CompactionResult.java
  - java/core/src/main/java/tech/amikos/chroma/local/core/WALPruneCollectionResult.java
  - java/core/src/main/java/tech/amikos/chroma/local/core/WALPruneResult.java
  - java/core/src/main/java/tech/amikos/chroma/local/core/BackupFileMetadata.java
  - java/core/src/main/java/tech/amikos/chroma/local/core/BackupManifest.java
  - java/core/src/test/java/tech/amikos/chroma/local/core/RebuildCollectionResultTest.java
  - java/core/src/test/java/tech/amikos/chroma/local/core/CompactionResultTest.java
  - java/core/src/test/java/tech/amikos/chroma/local/core/WALPruneResultTest.java
  - java/core/src/test/java/tech/amikos/chroma/local/core/BackupManifestTest.java
autonomous: true
requirements:
  - FOUND-01
  - FOUND-04

must_haves:
  truths:
    - "All 7 result POJOs compile in core module without JNA or Panama imports"
    - "Result POJOs deserialize from JSON matching Rust shim output format"
    - "Optional numeric fields use boxed Long and are null when absent from JSON"
    - "Gson shared instance uses LOWER_CASE_WITH_UNDERSCORES naming policy"
  artifacts:
    - path: "java/core/build.gradle.kts"
      provides: "Gson and SnakeYAML dependencies"
      contains: "com.google.code.gson:gson"
    - path: "java/core/src/main/java/tech/amikos/chroma/local/core/JsonUtil.java"
      provides: "Shared Gson instance"
      contains: "LOWER_CASE_WITH_UNDERSCORES"
    - path: "java/core/src/main/java/tech/amikos/chroma/local/core/RebuildCollectionResult.java"
      provides: "Rebuild result POJO"
      contains: "class RebuildCollectionResult"
    - path: "java/core/src/main/java/tech/amikos/chroma/local/core/CompactionResult.java"
      provides: "Compaction result POJO"
      contains: "class CompactionResult"
    - path: "java/core/src/main/java/tech/amikos/chroma/local/core/WALPruneResult.java"
      provides: "WAL prune result POJO"
      contains: "class WALPruneResult"
    - path: "java/core/src/main/java/tech/amikos/chroma/local/core/BackupManifest.java"
      provides: "Backup manifest POJO"
      contains: "class BackupManifest"
  key_links:
    - from: "java/core/src/main/java/tech/amikos/chroma/local/core/JsonUtil.java"
      to: "com.google.code.gson:gson"
      via: "Gson dependency in build.gradle.kts"
      pattern: "implementation.*gson"
    - from: "java/core/src/main/java/tech/amikos/chroma/local/core/CompactionResult.java"
      to: "java/core/src/main/java/tech/amikos/chroma/local/core/CompactionCollectionResult.java"
      via: "List<CompactionCollectionResult> collections field"
      pattern: "List<CompactionCollectionResult>"
---

<objective>
Add Gson and SnakeYAML dependencies to the core module and create all result POJOs for maintenance operations (rebuild, compaction, WAL prune, backup).

Purpose: Establish the data layer that all FFI responses deserialize into. These types are consumed by session methods in Phases 7-10. Per D-03, they are final-field classes with package-private constructors for Gson deserialization.
Output: 7 result POJO classes, 1 JSON utility class, updated build.gradle.kts, 4 test classes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-core-foundation-types/06-CONTEXT.md
@.planning/phases/06-core-foundation-types/06-RESEARCH.md

<interfaces>
<!-- Existing core module types the executor needs to know about -->

From java/core/src/main/java/tech/amikos/chroma/local/core/ChromaException.java:
```java
public final class ChromaException extends RuntimeException {
    public ChromaException(String message) { ... }
    public ChromaException(String message, Throwable cause) { ... }
}
```

From java/core/build.gradle.kts (current state):
```kotlin
plugins { `java-library` }
java { toolchain { languageVersion.set(JavaLanguageVersion.of(17)) } }
dependencies {
    testImplementation(platform("org.junit:junit-bom:5.11.4"))
    testImplementation("org.junit.jupiter:junit-jupiter")
    testRuntimeOnly("org.junit.platform:junit-platform-launcher")
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add dependencies and create JsonUtil</name>
  <files>
    java/core/build.gradle.kts,
    java/core/src/main/java/tech/amikos/chroma/local/core/JsonUtil.java
  </files>
  <read_first>
    java/core/build.gradle.kts,
    .planning/phases/06-core-foundation-types/06-CONTEXT.md,
    .planning/phases/06-core-foundation-types/06-RESEARCH.md
  </read_first>
  <action>
    1. Update `java/core/build.gradle.kts` to add two implementation dependencies (per D-02 and D-05):
       ```kotlin
       dependencies {
           implementation("com.google.code.gson:gson:2.13.2")
           implementation("org.yaml:snakeyaml:2.6")
           testImplementation(platform("org.junit:junit-bom:5.11.4"))
           testImplementation("org.junit.jupiter:junit-jupiter")
           testRuntimeOnly("org.junit.platform:junit-platform-launcher")
       }
       ```

    2. Create `java/core/src/main/java/tech/amikos/chroma/local/core/JsonUtil.java`:
       - Package: `tech.amikos.chroma.local.core`
       - Class: `final class JsonUtil` (package-private, not public)
       - Private constructor to prevent instantiation
       - Static final field `GSON` of type `com.google.gson.Gson`, created via:
         ```java
         static final Gson GSON = new GsonBuilder()
             .setFieldNamingPolicy(FieldNamingPolicy.LOWER_CASE_WITH_UNDERSCORES)
             .create();
         ```
       - Static method `<T> T fromJson(String json, Class<T> type)` that delegates to `GSON.fromJson(json, type)`
       - Static method `String toJson(Object obj)` that delegates to `GSON.toJson(obj)`
       - This class is package-private so only core module classes can use it
  </action>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/local-go-chroma/java && gradle --no-daemon :core:compileJava 2>&1 | tail -5</automated>
  </verify>
  <acceptance_criteria>
    - java/core/build.gradle.kts contains `implementation("com.google.code.gson:gson:2.13.2")`
    - java/core/build.gradle.kts contains `implementation("org.yaml:snakeyaml:2.6")`
    - JsonUtil.java contains `FieldNamingPolicy.LOWER_CASE_WITH_UNDERSCORES`
    - JsonUtil.java contains `static final Gson GSON`
    - JsonUtil.java contains `final class JsonUtil` (not `public final class`)
    - JsonUtil.java contains `static <T> T fromJson(`
    - JsonUtil.java contains `static String toJson(`
    - `:core:compileJava` succeeds
  </acceptance_criteria>
  <done>build.gradle.kts has both Gson 2.13.2 and SnakeYAML 2.6 deps; JsonUtil compiles with shared Gson instance using LOWER_CASE_WITH_UNDERSCORES policy</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Create all result POJOs with tests</name>
  <files>
    java/core/src/main/java/tech/amikos/chroma/local/core/RebuildCollectionResult.java,
    java/core/src/main/java/tech/amikos/chroma/local/core/CompactionCollectionResult.java,
    java/core/src/main/java/tech/amikos/chroma/local/core/CompactionResult.java,
    java/core/src/main/java/tech/amikos/chroma/local/core/WALPruneCollectionResult.java,
    java/core/src/main/java/tech/amikos/chroma/local/core/WALPruneResult.java,
    java/core/src/main/java/tech/amikos/chroma/local/core/BackupFileMetadata.java,
    java/core/src/main/java/tech/amikos/chroma/local/core/BackupManifest.java,
    java/core/src/test/java/tech/amikos/chroma/local/core/RebuildCollectionResultTest.java,
    java/core/src/test/java/tech/amikos/chroma/local/core/CompactionResultTest.java,
    java/core/src/test/java/tech/amikos/chroma/local/core/WALPruneResultTest.java,
    java/core/src/test/java/tech/amikos/chroma/local/core/BackupManifestTest.java
  </files>
  <read_first>
    java/core/src/main/java/tech/amikos/chroma/local/core/JsonUtil.java,
    java/core/src/main/java/tech/amikos/chroma/local/core/EmbeddedSession.java,
    internal/runtime/rebuild.go,
    internal/runtime/compaction.go,
    internal/runtime/wal_prune.go,
    internal/runtime/backup.go,
    .planning/phases/06-core-foundation-types/06-RESEARCH.md
  </read_first>
  <behavior>
    RebuildCollectionResultTest:
    - Deserializes full JSON with all fields populated -> all accessors return correct values
    - Deserializes JSON with missing optional fields (backupPath, warnings) -> backupPath is null, warnings is null or empty
    - collectionId() maps from JSON key "collection_id"
    - recordsScanned() returns long value from JSON key "records_scanned"

    CompactionResultTest:
    - Deserializes full CompactionResult with nested CompactionCollectionResult list
    - CompactionCollectionResult optional Long fields (pendingOpsBefore, pendingOpsAfter) are null when absent from JSON
    - CompactionCollectionResult optional Long fields have correct value when present
    - collectionCount() returns int from JSON key "collection_count"

    WALPruneResultTest:
    - Deserializes full WALPruneResult with nested WALPruneCollectionResult list
    - WALPruneCollectionResult optional Long fields (safeSeqCutoff, candidateSeqMin, candidateSeqMax, prunedSeqMin, prunedSeqMax) are null when absent
    - dryRun() returns boolean from JSON key "dry_run"
    - vacuumRequested() returns boolean from JSON key "vacuum_requested"

    BackupManifestTest:
    - Deserializes full BackupManifest with nested BackupFileMetadata list
    - Deserializes BackupManifest with empty/absent files list
    - BackupFileMetadata fields (path, sizeBytes, mode, sha256, modifiedAt) map correctly
    - fileCount() returns int, totalBytes() returns long
  </behavior>
  <action>
    Create 7 result POJO classes following this pattern for every class (per D-01, D-03, D-04):
    - `public final class` with all fields as `private final`
    - Package-private no-arg constructor that sets defaults (null for objects, 0 for primitives, false for booleans)
    - Public accessor methods using method-name style: `collectionId()` not `getCollectionId()` (per D-04)
    - Optional numeric fields (Go `*uint64`) use boxed `Long` in Java (per D-01)
    - No `@SerializedName` annotations -- rely on Gson `LOWER_CASE_WITH_UNDERSCORES` policy from JsonUtil

    **RebuildCollectionResult** (from Go `internal/runtime/rebuild.go` lines 14-27):
    Fields: `String collectionId`, `String name`, `String tenantId`, `String databaseName`, `boolean precheck`, `boolean wouldRebuild`, `boolean rebuilt`, `long recordsScanned`, `long vectorsReindexed`, `long durationMs`, `String backupPath`, `List<String> warnings`
    Accessors: `collectionId()`, `name()`, `tenantId()`, `databaseName()`, `precheck()`, `wouldRebuild()`, `rebuilt()`, `recordsScanned()`, `vectorsReindexed()`, `durationMs()`, `backupPath()`, `warnings()`

    **CompactionCollectionResult** (from Go `internal/runtime/compaction.go` lines 27-37):
    Fields: `String collectionId`, `String name`, `String tenantId`, `String databaseName`, `Long pendingOpsBefore` (boxed, nullable per D-01), `Long pendingOpsAfter` (boxed, nullable), `String pendingOpsBeforeError`, `String pendingOpsAfterError`, `String error`
    Accessors: matching field names as methods

    **CompactionResult** (from Go `internal/runtime/compaction.go` lines 40-47):
    Fields: `int collectionCount`, `long durationMs`, `long pendingOpsBeforeTotal`, `long pendingOpsAfterTotal`, `List<CompactionCollectionResult> collections`
    Accessors: matching field names as methods

    **WALPruneCollectionResult** (from Go `internal/runtime/wal_prune.go` lines 15-30):
    Fields: `String collectionId`, `String name`, `String tenantId`, `String databaseName`, `Long safeSeqCutoff` (boxed), `Long candidateSeqMin` (boxed), `Long candidateSeqMax` (boxed), `Long prunedSeqMin` (boxed), `Long prunedSeqMax` (boxed), `long candidateCount`, `long candidateBytes`, `long prunedCount`, `long prunedBytes`, `String error`
    Accessors: matching field names as methods

    **WALPruneResult** (from Go `internal/runtime/wal_prune.go` lines 33-45):
    Fields: `int collectionCount`, `long durationMs`, `boolean dryRun`, `boolean vacuumRequested`, `boolean vacuumExecuted`, `String warning`, `long candidateCountTotal`, `long candidateBytesTotal`, `long prunedCountTotal`, `long prunedBytesTotal`, `List<WALPruneCollectionResult> collections`
    Accessors: matching field names as methods

    **BackupFileMetadata** (from Go `internal/runtime/backup.go` lines 151-158):
    Fields: `String path`, `long sizeBytes`, `String mode`, `String sha256`, `String modifiedAt`
    Note: Go uses `time.Time` for modifiedAt but JSON serializes as ISO string; use `String` in Java.
    Accessors: matching field names as methods

    **BackupManifest** (from Go `internal/runtime/backup.go` lines 161-174):
    Fields: `String schemaVersion`, `String mode`, `String createdAt`, `String wrapperVersion`, `List<String> sourcePaths`, `String destinationPath`, `String snapshotPath`, `String manifestPath`, `boolean includeMetadata`, `int fileCount`, `long totalBytes`, `List<BackupFileMetadata> files`
    Accessors: matching field names as methods

    Create 4 test classes using hand-crafted inline JSON strings (per D-22/D-23):
    - Each test class uses `JsonUtil.fromJson(json, ResultType.class)` to deserialize
    - Test with full JSON (all fields present)
    - Test with JSON missing optional/nullable fields
    - Verify each accessor returns expected value
    - For nested types (CompactionResult -> CompactionCollectionResult), verify list deserialization
  </action>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/local-go-chroma/java && gradle --no-daemon :core:test 2>&1 | tail -20</automated>
  </verify>
  <acceptance_criteria>
    - RebuildCollectionResult.java contains `public final class RebuildCollectionResult`
    - RebuildCollectionResult.java contains `public String collectionId()` (not getCollectionId)
    - RebuildCollectionResult.java contains `RebuildCollectionResult()` (package-private constructor)
    - CompactionCollectionResult.java contains `private final Long pendingOpsBefore` (boxed Long, not primitive long)
    - WALPruneCollectionResult.java contains `private final Long safeSeqCutoff` (boxed Long)
    - BackupManifest.java contains `private final List<BackupFileMetadata> files`
    - BackupFileMetadata.java contains `public String sha256()`
    - RebuildCollectionResultTest.java contains `JsonUtil.fromJson`
    - CompactionResultTest.java contains `CompactionCollectionResult`
    - WALPruneResultTest.java contains `safeSeqCutoff`
    - BackupManifestTest.java contains `BackupFileMetadata`
    - `gradle :core:test` exits 0 with all tests passing
  </acceptance_criteria>
  <done>All 7 result POJOs compile and deserialize correctly from JSON; 4 test classes pass covering full fields, missing optional fields, nested list types, and boxed Long null semantics</done>
</task>

</tasks>

<verification>
1. `cd java && gradle --no-daemon :core:build` succeeds
2. `cd java && gradle --no-daemon :core:test` -- all tests pass
3. No JNA or Panama imports in any file under `java/core/src/main/`
4. All result POJOs have package-private no-arg constructors
5. All accessor methods use `name()` style, not `getName()` style
</verification>

<success_criteria>
- Gson 2.13.2 and SnakeYAML 2.6 are in core build.gradle.kts
- JsonUtil provides shared Gson instance with LOWER_CASE_WITH_UNDERSCORES policy
- 7 result POJO classes match Go field names and types exactly
- Optional numeric fields use boxed Long (null = absent)
- All 4 test classes pass with inline JSON deserialization tests
- Zero JNA or Panama imports in core module
</success_criteria>

<output>
After completion, create `.planning/phases/06-core-foundation-types/06-01-SUMMARY.md`
</output>
