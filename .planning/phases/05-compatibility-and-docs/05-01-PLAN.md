---
phase: 05-compatibility-and-docs
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - .github/workflows/ci.yml
autonomous: true
requirements:
  - DOCS-01
  - COMPAT-01
  - COMPAT-02

must_haves:
  truths:
    - "go-apidiff run against v0.3.4 reports only type-alias false positives, zero genuine breaking changes"
    - "CI workflow has an api-compat job that runs apidiff on PRs to main"
    - "Automated compatibility checks pass: race detector clean, cross-compile clean, import path resolves"
  artifacts:
    - path: ".github/workflows/ci.yml"
      provides: "api-compat job for apidiff on PRs to main"
      contains: "api-compat"
  key_links:
    - from: ".github/workflows/ci.yml"
      to: "golang.org/x/exp/cmd/apidiff"
      via: "go install in api-compat job"
      pattern: "go install golang.org/x/exp/cmd/apidiff"
---

<objective>
Run go-apidiff against the v0.3.4 tag to machine-verify API compatibility, document the expected type-alias false positives, add a CI apidiff job for PRs to main, and run the full automated compatibility checklist (race detector, cross-compile, import path verification).

Purpose: Provide machine-verified proof that the internal/ refactor introduced zero breaking API changes, and add ongoing CI protection for future PRs.
Output: Apidiff results documented, CI workflow updated with api-compat job, compatibility checklist items verified.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-compatibility-and-docs/05-CONTEXT.md
@.planning/phases/05-compatibility-and-docs/05-RESEARCH.md

<interfaces>
<!-- CI workflow structure executor needs to understand -->
<!-- From .github/workflows/ci.yml: 3 existing jobs -->
<!-- build-test-lint: 3-OS matrix, Go tests + lint + Rust lint -->
<!-- java-jna: 3-OS matrix, JNA smoke tests -->
<!-- java-panama: 3-OS matrix, Panama smoke tests -->
<!-- New api-compat job must be ADDED after existing jobs, not replace any -->

<!-- apidiff workflow from RESEARCH.md: -->
<!-- 1. go install golang.org/x/exp/cmd/apidiff@latest -->
<!-- 2. OLD_DIR=$(go mod download -json MODULE@TAG | jq -r .Dir) -->
<!-- 3. cd "$OLD_DIR" && apidiff -w /tmp/old.txt MODULE -->
<!-- 4. cd "$GITHUB_WORKSPACE" && apidiff -w /tmp/new.txt MODULE -->
<!-- 5. apidiff -incompatible /tmp/old.txt /tmp/new.txt -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run apidiff one-shot and automated compatibility checks</name>
  <files></files>
  <read_first>
    - .planning/phases/05-compatibility-and-docs/05-RESEARCH.md (apidiff workflow, false positive details)
    - compat_test.go (existing compile gate -- 110 symbols + 9 behavioral tests)
  </read_first>
  <action>
Run the full apidiff comparison workflow against v0.3.4 per D-01:

1. Install apidiff:
   ```
   go install golang.org/x/exp/cmd/apidiff@latest
   ```

2. Export old API surface (v0.3.4 from module cache):
   ```
   OLD_DIR=$(go mod download -json github.com/amikos-tech/chroma-go-local@v0.3.4 | jq -r .Dir)
   cd "$OLD_DIR"
   apidiff -w /tmp/apidiff_old.txt github.com/amikos-tech/chroma-go-local
   ```

3. Export new API surface (current working tree):
   ```
   cd /path/to/repo
   apidiff -w /tmp/apidiff_new.txt github.com/amikos-tech/chroma-go-local
   ```

4. Compare and capture output:
   ```
   apidiff -incompatible /tmp/apidiff_old.txt /tmp/apidiff_new.txt > /tmp/apidiff_results.txt 2>&1 || true
   ```

5. Review output: Per D-04, all reported "incompatible changes" should follow the pattern of type-alias false positives (e.g., "changed from X to internal/runtime.X"). Research found exactly 90 such false positives. If ANY line shows a genuinely different signature or a removed symbol, that IS a real breaking change and must be flagged.

6. Run the automated compatibility checklist items per D-10/D-11:
   - Race detector: `go test -race ./...` (requires shim: use `make test` which sets CHROMA_LIB_PATH)
   - Cross-compile: `GOOS=linux go build ./...`, `GOOS=darwin go build ./...`, `GOOS=windows go build ./...`
   - Import path: already verified by compat_test.go compile gate (110 symbols) -- confirm `go build ./...` passes
   - compat_test.go: confirm it compiles and the 110-symbol gate + 9 behavioral tests pass (part of `make test`)

7. Clean up temp files: `rm -f /tmp/apidiff_old.txt /tmp/apidiff_new.txt /tmp/apidiff_results.txt`

No files are created or modified by this task -- it is a verification-only task that confirms the state of the codebase.
  </action>
  <verify>
    <automated>go build ./... && GOOS=linux go build ./... && GOOS=windows go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - apidiff -incompatible output contains ONLY type-alias false positives (pattern: "changed from X to internal/runtime.X")
    - Zero genuine breaking changes (no removed symbols, no genuinely different signatures)
    - `go build ./...` exits 0
    - `GOOS=linux go build ./...` exits 0
    - `GOOS=darwin go build ./...` exits 0
    - `GOOS=windows go build ./...` exits 0
    - `make test` exits 0 (includes race detector via go test and compat_test.go)
  </acceptance_criteria>
  <done>apidiff confirms only type-alias false positives, all automated compatibility checks pass (cross-compile, race detector, compile gate)</done>
</task>

<task type="auto">
  <name>Task 2: Add api-compat CI job for PRs to main</name>
  <files>.github/workflows/ci.yml</files>
  <read_first>
    - .github/workflows/ci.yml (current CI workflow with 3 jobs: build-test-lint, java-jna, java-panama)
    - .planning/phases/05-compatibility-and-docs/05-RESEARCH.md (CI apidiff job template)
    - .planning/phases/05-compatibility-and-docs/05-CONTEXT.md (D-02, D-03 decisions)
  </read_first>
  <action>
Add a new `api-compat` job to `.github/workflows/ci.yml` per D-02 and D-03. The job runs ONLY on PRs to main (not every push). It dynamically resolves the latest release tag and runs apidiff as an informational check (warning, not failure) per Pitfall 5 from RESEARCH.md.

Append the following job AFTER the existing `java-panama` job in ci.yml. Do NOT modify any existing jobs.

```yaml
  api-compat:
    name: API Compatibility Check
    if: github.event_name == 'pull_request' && github.base_ref == 'main'
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2
        with:
          fetch-depth: 0
      - uses: actions/setup-go@4b73464bb391d4059bd26b0524d20df3927bd417 # v6.3.0
        with:
          go-version-file: go.mod
          cache: true
      - name: Run API compatibility check
        run: |
          go install golang.org/x/exp/cmd/apidiff@latest
          LATEST_TAG=$(git tag -l 'v*' --sort=-v:refname | head -1)
          if [ -z "$LATEST_TAG" ]; then
            echo "No release tags found -- skipping apidiff"
            exit 0
          fi
          echo "Comparing against: $LATEST_TAG"
          OLD_DIR=$(go mod download -json "github.com/amikos-tech/chroma-go-local@${LATEST_TAG}" | jq -r .Dir)
          cd "$OLD_DIR" && apidiff -w /tmp/old.txt github.com/amikos-tech/chroma-go-local
          cd "$GITHUB_WORKSPACE" && apidiff -w /tmp/new.txt github.com/amikos-tech/chroma-go-local
          echo "## API Diff (vs $LATEST_TAG)" >> "$GITHUB_STEP_SUMMARY"
          if apidiff -incompatible /tmp/old.txt /tmp/new.txt > /tmp/diff.txt 2>&1; then
            echo "No incompatible changes detected." >> "$GITHUB_STEP_SUMMARY"
          else
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            cat /tmp/diff.txt >> "$GITHUB_STEP_SUMMARY"
            echo '```' >> "$GITHUB_STEP_SUMMARY"
            echo "::warning::API changes detected vs $LATEST_TAG - review step summary"
          fi
```

Key details:
- Job name: `api-compat` (per RESEARCH.md recommendation)
- Condition: `github.event_name == 'pull_request' && github.base_ref == 'main'` (per D-03: PRs to main only)
- Uses same pinned action versions as existing jobs (checkout v6.0.2, setup-go v6.3.0)
- Does NOT fail the build -- uses `::warning` annotation (per Pitfall 5: type-alias false positives are expected)
- Dynamically resolves latest release tag via `git tag -l 'v*' --sort=-v:refname | head -1`
- Gracefully skips if no tags exist (new repos)
- Writes results to `$GITHUB_STEP_SUMMARY` for visibility

Validate the updated ci.yml with yq:
```
yq '.' .github/workflows/ci.yml > /dev/null
```
  </action>
  <verify>
    <automated>yq '.jobs.api-compat.name' .github/workflows/ci.yml</automated>
  </verify>
  <acceptance_criteria>
    - `.github/workflows/ci.yml` contains a job named `api-compat`
    - `yq '.jobs.api-compat.name' .github/workflows/ci.yml` outputs "API Compatibility Check"
    - `yq '.jobs.api-compat.if' .github/workflows/ci.yml` contains "pull_request" and "main"
    - The api-compat job step contains `go install golang.org/x/exp/cmd/apidiff@latest`
    - The api-compat job step contains `::warning` (informational, not failure)
    - The api-compat job step contains `GITHUB_STEP_SUMMARY`
    - Existing jobs (build-test-lint, java-jna, java-panama) are unchanged
    - `yq '.' .github/workflows/ci.yml > /dev/null` exits 0 (valid YAML)
  </acceptance_criteria>
  <done>CI workflow has api-compat job that runs apidiff on PRs to main, dynamically resolves latest tag, reports as warning not failure</done>
</task>

</tasks>

<verification>
- `yq '.jobs | keys' .github/workflows/ci.yml` shows 4 jobs including api-compat
- `go build ./...` passes (API surface intact)
- Cross-compile passes for linux, darwin, windows
</verification>

<success_criteria>
- go-apidiff confirmed only type-alias false positives vs v0.3.4
- All automated compatibility checks pass (race detector, cross-compile, compile gate)
- CI workflow updated with api-compat job for PRs to main
- api-compat job is informational only (warning, not failure)
</success_criteria>

<output>
After completion, create `.planning/phases/05-compatibility-and-docs/05-01-SUMMARY.md`
</output>
