---
phase: 05-compatibility-and-docs
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - README.md
  - CLAUDE.md
  - GO_API_SURFACE.md
  - CHANGELOG.md
autonomous: true
requirements:
  - DOCS-02
  - DOCS-03
  - DOCS-04
  - COMPAT-03

must_haves:
  truths:
    - "README.md describes the facade + internal layout, not the old flat root layout"
    - "CLAUDE.md architecture diagram shows root facade -> internal/runtime + internal/library"
    - "CLAUDE.md Key Patterns section includes facade pattern note"
    - "GO_API_SURFACE.md file path references point to internal/runtime, not root"
    - "CHANGELOG.md exists with v0.4.0 entry including compatibility statement"
  artifacts:
    - path: "README.md"
      provides: "Updated Project Structure and architecture note"
      contains: "internal/runtime/"
    - path: "CLAUDE.md"
      provides: "Updated architecture diagram and facade pattern note"
      contains: "internal/runtime/"
    - path: "GO_API_SURFACE.md"
      provides: "Updated file path references"
      contains: "internal/runtime/"
    - path: "CHANGELOG.md"
      provides: "v0.4.0 release entry with compatibility statement"
      contains: "No breaking changes"
  key_links:
    - from: "README.md"
      to: "internal/runtime/"
      via: "Project Structure section"
      pattern: "internal/runtime/"
    - from: "CLAUDE.md"
      to: "internal/runtime/"
      via: "Architecture diagram"
      pattern: "internal/runtime/"
---

<objective>
Update all project documentation (README.md, CLAUDE.md, GO_API_SURFACE.md) to reflect the new internal/ layout, and create CHANGELOG.md with the v0.4.0 release entry including the compatibility statement.

Purpose: Ensure documentation accurately describes the post-refactor architecture so contributors and users understand the facade + internal pattern, and prepare release notes for v0.4.0.
Output: Four files updated/created with accurate paths, architecture descriptions, and release notes.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/05-compatibility-and-docs/05-CONTEXT.md
@.planning/phases/05-compatibility-and-docs/05-RESEARCH.md

<interfaces>
<!-- Current file locations that documentation must reference: -->
<!-- Root facade files: chroma.go, config.go, embedded.go, errors.go, backup.go, rebuild.go, compaction.go, wal_prune.go, doc.go -->
<!-- Implementation: internal/runtime/ (8 files: chroma.go, config.go, embedded.go, errors.go, backup.go, rebuild.go, compaction.go, wal_prune.go) -->
<!-- FFI loading: internal/library/ (3 files: library.go, library_unix.go, library_windows.go) -->
<!-- Tests: compat_test.go at root, 11 test files in internal/runtime/, 1 in internal/library/ -->
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update README.md, CLAUDE.md, and GO_API_SURFACE.md</name>
  <files>README.md, CLAUDE.md, GO_API_SURFACE.md</files>
  <read_first>
    - README.md (full file -- Project Structure at lines 664-688, architecture note insertion point after "Building" section around line 49)
    - CLAUDE.md (full file -- Architecture diagram at lines 48-54, Key Patterns at lines 61-76)
    - GO_API_SURFACE.md (full file -- scan for any "defined in chroma.go" or root-level file path references)
    - .planning/phases/05-compatibility-and-docs/05-RESEARCH.md (exact replacement content for each file)
    - .planning/phases/05-compatibility-and-docs/05-CONTEXT.md (D-05 through D-08 decisions)
  </read_first>
  <action>
**README.md changes (per D-05):**

1. Replace the "Project Structure" section (lines 664-688) with the updated layout that shows facade + internal split. Replace the entire code block between the ``` markers with:

```
.
├── chroma.go        # Root facade: type aliases + thin wrappers
├── config.go        # Facade: ServerConfig, ServerOption
├── embedded.go      # Facade: Embedded, EmbeddedOption, request/response types
├── errors.go        # Facade: error types and sentinel errors
├── backup.go        # Facade: BackupOption, BackupManifest
├── rebuild.go       # Facade: RebuildCollectionOption, RebuildCollectionResult
├── compaction.go    # Facade: CompactCollectionRequest (methods via type alias)
├── wal_prune.go     # Facade: WALPruneOption, WALPruneResult
├── doc.go           # Package documentation
├── compat_test.go   # Compile-time API surface gate (110 symbols + 9 behavioral)
├── internal/
│   ├── runtime/     # Server, embedded, config, backup, rebuild, compaction, WAL prune
│   └── library/     # FFI loading via purego (platform-specific)
├── java/            # Java scaffold modules (core, jna, panama)
├── shim/            # Rust FFI shim
├── examples/        # Go and Java usage examples
├── scripts/         # Dev helpers (Windows, backfill)
└── Makefile         # Build orchestration
```

2. Add an architecture note after the "Building" section (after line 49, before "## Java Scaffold"). Insert a new section:

```markdown
## Architecture

The root package (`github.com/amikos-tech/chroma-go-local`) is a thin facade that re-exports all public symbols from internal implementation packages via type aliases and wrapper functions. Implementation lives in `internal/runtime/` (server lifecycle, embedded mode, config, errors, backup, rebuild, compaction, WAL prune) and `internal/library/` (FFI loading via purego, platform-specific shims). The root package contains zero implementation logic.
```

**CLAUDE.md changes (per D-07, D-08):**

1. Replace the Architecture diagram (lines 48-53, the code block) with the updated diagram that shows facade -> internal layout:

```
Go Package (root)                   Internal Implementation
├── chroma.go     ─── facade ───►   internal/runtime/
├── config.go         (type         ├── chroma.go      (server lifecycle)
├── embedded.go        aliases      ├── config.go      (builder options)
├── errors.go          + thin       ├── embedded.go    (embedded mode)
├── backup.go          wrappers)    ├── errors.go      (error types)
├── rebuild.go                      ├── backup.go      (backup API)
├── compaction.go                   ├── rebuild.go     (rebuild API)
├── wal_prune.go                    ├── compaction.go  (compaction API)
└── doc.go                          └── wal_prune.go   (WAL prune API)
                                    internal/library/
Rust Shim (shim/)                   ├── library.go     (FFI loading)
└── src/lib.rs ◄────────────────    ├── library_unix.go
    (chroma_* symbols)              └── library_windows.go

Java scaffold (java/)
├── core   (shared API models)
├── jna    (Java 17 fallback)
└── panama (Java 22 primary)
```

2. After the existing "Resource cleanup" bullet point (around line 59), add:
```
- **Facade pattern**: Root package re-exports all symbols via type aliases (`type X = runtime.X`) and thin wrapper functions; zero implementation logic at root
```

3. In the "Key Patterns" section (after the YAML string config example, around line 76), add a new subsection:

```markdown
Facade pattern (root package):
```go
// Type aliases forward types from internal/runtime
type Server = runtime.Server
type ServerOption = runtime.ServerOption

// Thin wrappers forward functions
var NewServer = runtime.NewServer
var StartServer = runtime.StartServer
```

The root package contains zero logic -- all implementation lives in `internal/runtime/` and `internal/library/`.
```

**GO_API_SURFACE.md changes (per D-06):**

Update file path references only -- no content or example changes. The document does not contain many explicit file path references, but scan for and update:
- Any reference to "defined in chroma.go" should become "defined in internal/runtime/chroma.go, re-exported at root"
- Any reference to "defined in config.go" should become "defined in internal/runtime/config.go, re-exported at root"
- Any reference to "defined in embedded.go" should become "defined in internal/runtime/embedded.go, re-exported at root"
- Any reference to "defined in errors.go" should become "defined in internal/runtime/errors.go, re-exported at root"

If no such explicit references exist in the body text (research indicates they are mostly implicit), add a brief note at the top of the file after the title:

```markdown
> **Note:** All types and functions below are defined in `internal/runtime/` and `internal/library/`, re-exported at the root package (`github.com/amikos-tech/chroma-go-local`). Import the root package -- never import internal packages directly.
```
  </action>
  <verify>
    <automated>grep -c 'internal/runtime/' README.md && grep -c 'internal/runtime/' CLAUDE.md && grep -c 'internal/runtime/' GO_API_SURFACE.md</automated>
  </verify>
  <acceptance_criteria>
    - README.md "Project Structure" section contains `internal/runtime/` and `internal/library/`
    - README.md "Project Structure" section does NOT contain `library.go      # Library loading via purego` (old flat layout)
    - README.md contains `## Architecture` section with text "thin facade"
    - CLAUDE.md Architecture code block contains `internal/runtime/` and `internal/library/`
    - CLAUDE.md Architecture code block does NOT contain the old 4-line diagram (`├── library.go`)
    - CLAUDE.md contains "Facade pattern" in the bullet list below architecture
    - CLAUDE.md Key Patterns section contains `type Server = runtime.Server`
    - GO_API_SURFACE.md contains `internal/runtime/` (either in updated references or in the note)
    - `go build ./...` still passes (no accidental code changes)
  </acceptance_criteria>
  <done>README.md, CLAUDE.md, and GO_API_SURFACE.md all reflect the new internal/ layout with accurate file paths and architecture descriptions</done>
</task>

<task type="auto">
  <name>Task 2: Create CHANGELOG.md with v0.4.0 release entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - .planning/phases/05-compatibility-and-docs/05-RESEARCH.md (CHANGELOG format template, compatibility statement wording)
    - .planning/phases/05-compatibility-and-docs/05-CONTEXT.md (D-12, D-13, D-14 decisions)
  </read_first>
  <action>
Create a new `CHANGELOG.md` at the repository root per D-12. Use "Keep a Changelog" format (https://keepachangelog.com/). Per D-14, no roadmap hints or forward-looking notes. Per D-13, include the exact compatibility statement: "No breaking changes. Your existing code using github.com/amikos-tech/chroma-go-local requires zero modifications." Per Pitfall 4, use "UNRELEASED" as the date placeholder.

Create `CHANGELOG.md` with this exact content:

```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/),
and this project adheres to [Semantic Versioning](https://semver.org/).

## [0.4.0] - UNRELEASED

### Changed
- Internal structure reorganized: Go implementation moved from repo root
  into `internal/runtime/` and `internal/library/` subtrees. The root
  package is now a thin facade that re-exports all public symbols via
  type aliases and function wrappers.

### Compatibility
- No breaking changes. Your existing code using
  `github.com/amikos-tech/chroma-go-local` requires zero modifications.
- Import path unchanged: `github.com/amikos-tech/chroma-go-local`
- All 110+ exported symbols preserved with identical signatures
- Verified via `go-apidiff` against v0.3.4 baseline

[0.4.0]: https://github.com/amikos-tech/chroma-go-local/compare/v0.3.4...v0.4.0
```
  </action>
  <verify>
    <automated>test -f CHANGELOG.md && grep -c 'No breaking changes' CHANGELOG.md && grep -c 'UNRELEASED' CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `CHANGELOG.md` exists at repository root
    - CHANGELOG.md contains `# Changelog` as first line
    - CHANGELOG.md contains `## [0.4.0] - UNRELEASED`
    - CHANGELOG.md contains `No breaking changes. Your existing code using`
    - CHANGELOG.md contains `github.com/amikos-tech/chroma-go-local` in compatibility statement
    - CHANGELOG.md contains `requires zero modifications` (exact wording per D-13)
    - CHANGELOG.md contains `Keep a Changelog` format attribution
    - CHANGELOG.md contains the comparison link `[0.4.0]: https://github.com/amikos-tech/chroma-go-local/compare/v0.3.4...v0.4.0`
    - CHANGELOG.md does NOT contain any roadmap hints or forward-looking notes (per D-14)
  </acceptance_criteria>
  <done>CHANGELOG.md exists with v0.4.0 entry in Keep a Changelog format, UNRELEASED date placeholder, exact compatibility statement per D-13, comparison link, no forward-looking content</done>
</task>

</tasks>

<verification>
- `grep 'internal/runtime/' README.md` returns matches in Project Structure and Architecture sections
- `grep 'internal/runtime/' CLAUDE.md` returns matches in Architecture diagram and Key Patterns
- `grep 'internal/runtime/' GO_API_SURFACE.md` returns at least one match
- `test -f CHANGELOG.md` exits 0
- `grep 'No breaking changes' CHANGELOG.md` returns a match
- `go build ./...` still passes (no accidental code changes in documentation updates)
</verification>

<success_criteria>
- README.md Project Structure shows facade + internal layout (not old flat layout)
- README.md has Architecture section explaining facade pattern
- CLAUDE.md has updated architecture diagram with internal/runtime and internal/library
- CLAUDE.md Key Patterns includes facade pattern note with code example
- GO_API_SURFACE.md references internal/runtime file paths
- CHANGELOG.md exists with v0.4.0 entry, compatibility statement, UNRELEASED date
- All four files contain no stale references to the old flat layout
</success_criteria>

<output>
After completion, create `.planning/phases/05-compatibility-and-docs/05-02-SUMMARY.md`
</output>
