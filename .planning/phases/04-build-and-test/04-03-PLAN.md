---
phase: 04-build-and-test
plan: 03
type: execute
wave: 2
depends_on:
  - 04-01
  - 04-02
files_modified: []
autonomous: false
requirements:
  - TEST-01
  - TEST-04
  - BUILD-01
  - BUILD-02
  - BUILD-04

must_haves:
  truths:
    - "make test passes with reorganized test layout including compat_test.go at root"
    - "make test-release passes with reorganized test layout including compat_test.go at root"
    - "make test-all passes (Go + Rust + Java smoke tests)"
    - "make lint passes with zero issues"
    - "Cross-compile succeeds for linux, darwin, and windows"
    - "CI workflow requires zero modifications to work with the new layout"
    - "Implementation tests exist alongside internal packages (12 test files in internal/)"
  artifacts:
    - path: "Makefile"
      provides: "Unchanged make targets that work with new layout"
      contains: "go test -v ./..."
    - path: ".github/workflows/ci.yml"
      provides: "Unchanged CI workflow that traverses new layout"
      contains: "go test -v ./..."
  key_links:
    - from: "Makefile"
      to: "go test -v ./..."
      via: "RUN_GO_TEST_DEBUG variable"
      pattern: "go test -v \\./\\.\\.\\."
    - from: ".github/workflows/ci.yml"
      to: "go test -v ./..."
      via: "Run Go tests step"
      pattern: "go test -v \\./\\.\\.\\."
---

<objective>
Verify that all build, test, lint, and cross-compile targets pass cleanly after the lint fixes (Plan 01) and compat_test.go creation (Plan 02). This is a pure verification plan -- no files are modified.

Purpose: Gate the phase by confirming every success criterion from the ROADMAP is met. Per D-09 (make test unchanged), D-11 (no structural Makefile changes), D-12 (zero CI changes).
Output: Verification results confirming all 5 requirements (TEST-01, TEST-04, BUILD-01, BUILD-02, BUILD-04) pass.
</objective>

<execution_context>
@~/.claude/get-shit-done/workflows/execute-plan.md
@~/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-build-and-test/04-CONTEXT.md
@.planning/phases/04-build-and-test/04-RESEARCH.md
@.planning/phases/04-build-and-test/04-01-SUMMARY.md
@.planning/phases/04-build-and-test/04-02-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Run full verification suite</name>
  <files>N/A - verification only, no files modified</files>
  <read_first>
    - Makefile
    - .github/workflows/ci.yml
    - .golangci.yml
    - compat_test.go
  </read_first>
  <action>
Execute the following verification commands in sequence. Each must exit 0. If any fails, diagnose and report the failure but do NOT modify source files (those belong to Plans 01 and 02).

**Step 1: Verify internal test files exist (TEST-01)**
Run: `ls internal/runtime/*_test.go internal/library/*_test.go | wc -l`
Expected: 12 (11 in internal/runtime/ + 1 in internal/library/)

**Step 2a: make test (TEST-04, BUILD-01)**
Run: `make test`
This builds the debug Rust shim and then runs `CHROMA_LIB_PATH=... go test -v ./...`, which traverses root (compat_test.go) + internal packages. Must exit 0.

**Step 2b: make test-release (ROADMAP success criterion 1)**
Run: `make test-release`
This builds the release Rust shim and runs the same Go test suite against it. Must exit 0.

**Step 2c: make test-all (ROADMAP success criterion 1)**
Run: `make test-all`
This runs Go + Rust + Java smoke tests. Must exit 0. (Java tests are skipped if Gradle is missing, per Makefile logic.)

**Step 3: make lint (BUILD-01, BUILD-03 verification)**
Run: `make lint`
This runs `golangci-lint run ./...` and `cargo clippy`. Must exit 0 with zero Go lint issues.

**Step 4: Cross-compile verification (BUILD-04)**
Run these three commands:
- `GOOS=linux go build ./...`
- `GOOS=darwin go build ./...`
- `GOOS=windows go build ./...`
All three must exit 0.

**Step 5: CI workflow inspection (BUILD-02)**
Read `.github/workflows/ci.yml` and verify:
- The `go test -v ./...` command is present (line 78)
- The OS matrix includes ubuntu-latest, macos-latest, windows-latest
- No changes are needed -- the workflow already traverses the new layout
  </action>
  <verify>
    <automated>make test && make test-release && make test-all && make lint && GOOS=linux go build ./... && GOOS=darwin go build ./... && GOOS=windows go build ./...</automated>
  </verify>
  <acceptance_criteria>
    - `make test` exits 0 with all tests passing (root compat_test.go + internal tests)
    - `make test-release` exits 0 with all tests passing against release shim
    - `make test-all` exits 0 (Go + Rust + Java smoke tests)
    - `make lint` exits 0 with zero Go lint issues
    - `GOOS=linux go build ./...` exits 0
    - `GOOS=darwin go build ./...` exits 0
    - `GOOS=windows go build ./...` exits 0
    - `ls internal/runtime/*_test.go internal/library/*_test.go | wc -l` outputs 12
    - .github/workflows/ci.yml contains `go test -v ./...` (unchanged)
    - Makefile contains `go test -v ./...` in RUN_GO_TEST_DEBUG (unchanged)
  </acceptance_criteria>
  <done>All make targets (test, test-release, test-all, lint) and cross-compile pass; CI workflow needs no changes; 12 internal test files confirmed alongside their packages</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Confirm phase success criteria</name>
  <files>N/A - human verification only</files>
  <action>
Human verification of Phase 4 completion. All automated checks passed in Task 1. User confirms the phase deliverables are correct by inspecting the results and optionally running the verification commands themselves.
  </action>
  <what-built>
Phase 4 deliverables:
1. Fixed gci prefix in .golangci.yml (BUILD-03)
2. Added nolint:staticcheck to deprecated type re-exports in backup.go
3. Created compat_test.go with 87-symbol compile gate + 9 behavioral smoke tests (TEST-02, TEST-03)
4. Verified make test, make test-release, make test-all, make lint, cross-compile all pass (TEST-01, TEST-04, BUILD-01, BUILD-02, BUILD-04)
  </what-built>
  <how-to-verify>
1. Run `make test` -- all tests pass including new compat_test.go
2. Run `make test-release` -- all tests pass against release shim
3. Run `make test-all` -- Go + Rust + Java smoke tests pass
4. Run `make lint` -- zero lint issues
5. Run `GOOS=linux go build ./... && GOOS=darwin go build ./... && GOOS=windows go build ./...` -- all succeed
6. Inspect `compat_test.go` -- verify it references all exported symbols from the facade
7. Inspect `.github/workflows/ci.yml` -- confirm no changes were needed
  </how-to-verify>
  <verify>
    <automated>make test && make test-release && make test-all && make lint</automated>
  </verify>
  <done>Human confirms all Phase 4 success criteria are met</done>
  <resume-signal>Type "approved" or describe issues</resume-signal>
</task>

</tasks>

<verification>
Phase 4 success criteria from ROADMAP.md:
1. `make test`, `make test-release`, `make lint`, and `make test-all` all pass locally
2. `golangci-lint run ./...` reports zero issues; stale gci prefix corrected
3. Cross-compile succeeds for linux, darwin, windows
4. `compat_test.go` exists at repo root in `package chroma_test` referencing every exported symbol
5. CI workflows run without modification to OS matrix or job structure
</verification>

<success_criteria>
- All 5 ROADMAP success criteria confirmed
- All 8 phase requirements (TEST-01 through TEST-04, BUILD-01 through BUILD-04) verified
- Human approves phase completion
</success_criteria>

<output>
After completion, create `.planning/phases/04-build-and-test/04-03-SUMMARY.md`
</output>
