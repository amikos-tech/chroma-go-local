---
phase: 11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9
plan: "03"
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - README.md
  - AGENTS.md
  - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-after.exports
  - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-export-comparison.md
autonomous: true
requirements:
  - UPG-03
  - UPG-04
must_haves:
  truths:
    - "Source builders know the measured Rust 1.88.0/protoc 31.1 contract, CI/releases retain Rust 1.93.1, and prebuilt consumers need neither (D-06); README and AGENTS.md agree."
    - "The rebuilt native chroma_* export set exactly matches the captured pre-migration set; unavailable local inspection is precisely recorded as a blocking Plan 11-04 supported-runner ABI gate."
    - "`make test` and `make test-java` exercise fresh-data Go, JNA, and Panama paths against a newly rebuilt shim, without an existing-data compatibility claim (D-08, D-09)."
    - "Phase 13 owns all five named additive APIs: UUID collection lookup, `fork_count`, bounded-WAL reads via `ReadLevel::IndexAndBoundedWal`, sparse-index selection, and MaxScore controls (D-01)."
    - "Phase 11 adds no C/Go/JNA/Panama typed API and preserves default-compatible raw YAML/schema parsing while the existing bindings remain backward compatible (D-02)."
    - "Phase 13 owns all deferred additive APIs; Phase 12 owns persisted-data upgrade, mutation/reopen, and maintenance compatibility."
  artifacts:
    - path: README.md
      provides: "User-facing source-build, CI/release, prebuilt-consumer, and compatibility-boundary guidance."
      contains: "Rust 1.88.0"
    - path: AGENTS.md
      provides: "Accurate project-agent Rust source-build MSRV and CI/release pin guidance."
      contains: "Rust 1.88.0"
    - path: .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-after.exports
      provides: "Sorted actual post-migration chroma_* exports when inspection is available."
    - path: .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-export-comparison.md
      provides: "Before/after ABI comparison or exact unavailability record."
  key_links:
    - from: "rebuilt chroma_shim dynamic library"
      to: "Go purego, JNA, and Panama"
      via: "make test and make test-java set/use the rebuilt CHROMA_LIB_PATH"
      pattern: "CHROMA_LIB_PATH"
    - from: 11-abi-before.exports
      to: 11-abi-after.exports
      via: "sorted diff of actual native exports"
      pattern: "^chroma_"
---

<objective>
Publish the real 1.5.9 source-build contract, compare actual native exports before and after migration, and run bounded fresh-data binding validation.

Purpose: The upgrade must be understandable to contributors and demonstrably preserve the artifact all bindings load, while retaining the explicit Phase 12/13 scope boundary.
Output: README guidance, native ABI comparison evidence, and accurately bounded local validation results.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-RESEARCH.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-VALIDATION.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-PATTERNS.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-01-SUMMARY.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-02-SUMMARY.md
</context>

## Source Coverage Audit

| Source | Item | Coverage |
|--------|------|----------|
| GOAL | Chroma 1.5.9 graph, one private signature adaptation, preserved binding contracts | 11-01 through 11-04: resolve → adapt → artifact/binding evidence → exact-SHA CI evidence |
| REQ | UPG-01 | 11-01 semantic resolver and locked compiler check |
| REQ | UPG-02 | 11-02 private empty-region adaptation and public delete/survivor test |
| REQ | UPG-03 | 11-01/03 native artifact comparison, 11-03 Go/JNA/Panama tests, 11-04 cross-platform CI evidence |
| REQ | UPG-04 | 11-01 MSRV declaration/check and 11-03 contributor guidance |
| RESEARCH | metadata predicate, export diff, real Make targets, CI metadata discovery, no-cgo | Explicit tasks below; no text-only substitutes |
| CONTEXT | D-01/D-02 deferred API/no new public surface | README scope record; no public C/Go/JNA/Panama edit task |
| CONTEXT | D-03/D-04 delete behavior | 11-02 only |
| CONTEXT | D-05/D-07 constrained graph/revalidation | 11-01/02 only |
| CONTEXT | D-06 hybrid tools | README and unchanged workflow cross-check |
| CONTEXT | D-08/D-09 fresh-data-only/Phase 12 release gate | Local and CI evidence explicitly bounded in 11-03/04 |

<tasks>

<task type="auto">
  <name>Task 1: Align contributor and project-agent toolchain guidance without changing workflows</name>
  <files>README.md, AGENTS.md</files>
  <read_first>
    - README.md — requirements, Windows setup, testing, CI, and troubleshooting wording to update
    - AGENTS.md — Requirements and build guidance that must distinguish the Rust 1.88.0 source-build MSRV from the Rust 1.93.1 CI/release pin
    - .github/workflows/ci.yml and .github/workflows/release.yml — existing exact Rust 1.93.1 and protoc 31.1 pins to cross-check, not reformat
    - .planning/spikes/002-chroma-1-5-9-toolchain-floor/README.md — validated Rust 1.88.0/protoc 31.1 evidence
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md — D-01, D-02, D-06, D-08, and D-09
  </read_first>
  <action>Update `README.md` contributor-facing text and `AGENTS.md` Requirements/build guidance. Replace stale Rust 1.70+ and Chroma 1.5.5/protoc 31.x source-build wording with D-06's hybrid contract: Rust 1.88.0 is the source-build MSRV for the committed graph, `protoc` 31.1 is required to build the shim from source, and CI/releases retain their existing exact Rust 1.93.1 pin. In `AGENTS.md`, expressly distinguish the 1.88.0 source-build MSRV from the 1.93.1 CI/release pin while preserving every other instruction. State plainly in README that consumers of released/prebuilt native libraries need neither Rust nor protoc. Preserve the established Go, Java, lint, Windows, and no-cgo guidance. Add a concise migration-boundary note: D-01/D-02 defer UUID collection lookup, `fork_count`, `ReadLevel::IndexAndBoundedWal`, sparse-index selection, and MaxScore controls to Phase 13 with no new C, Go, JNA, or Panama typed API here; D-08/D-09 limit Phase 11 to fresh-data evidence and reserve persisted 1.5.5-data upgrade, mutation/reopen, and maintenance compatibility for Phase 12. Do not edit existing CI/release workflows merely to restate their already-correct pins. Addresses review concern: source-builder guidance must be consistent for contributors and the project agents that follow `AGENTS.md`.</action>
  <verify>
    <automated>rg -q 'Rust 1.88.0' README.md AGENTS.md &amp;&amp; rg -q 'Rust 1.93.1' README.md AGENTS.md &amp;&amp; rg -q 'protoc 31.1' README.md &amp;&amp; rg -qi 'prebuilt.*neither Rust nor protoc\|neither Rust nor protoc.*prebuilt' README.md &amp;&amp; rg -q 'Phase 12' README.md &amp;&amp; rg -q 'Phase 13' README.md &amp;&amp; ! rg -q 'Rust 1.70+' README.md AGENTS.md &amp;&amp; rg -q 'toolchain: 1.93.1' .github/workflows/ci.yml .github/workflows/release.yml &amp;&amp; rg -q 'PROTOC_VERSION: "31.1"' .github/workflows/ci.yml .github/workflows/release.yml &amp;&amp; if command -v shellcheck &gt;/dev/null 2&gt;&amp;1 &amp;&amp; command -v yamllint &gt;/dev/null 2&gt;&amp;1; then make lint-workflows; else printf 'UNAVAILABLE: make lint-workflows prerequisites; record command -v shellcheck/yamllint output in 11-03-SUMMARY.md\\n'; fi</automated>
  </verify>
  <acceptance_criteria>README and AGENTS.md accurately distinguish source builders, CI/releases, and prebuilt consumers; identify all five deferred capability groups without adding a binding API; and clearly limit Phase 11 evidence to fresh data while Phase 12 owns cross-version persisted-data proof. The executor records a semantic documentation-review checklist in `11-03-SUMMARY.md` covering Rust 1.88.0/protoc 31.1 source builders, Rust 1.93.1 CI/releases, and prebuilt consumers needing neither; it runs `make lint-workflows` when ShellCheck and yamllint are available, otherwise records their exact discovery output. No workflow edit is made without a demonstrated mismatch.</acceptance_criteria>
  <done>Contributor and project-agent guidance match the validated graph, workflow lint or its precise prerequisite limitation is recorded, and no user-facing API is expanded.</done>
</task>

<task type="auto">
  <name>Task 2: Compare built exports and run the rebuilt-shim fresh-data suites</name>
  <files>.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-after.exports, .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-export-comparison.md</files>
  <read_first>
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-before.exports and 11-abi-before-inspection.md — baseline list or exact availability outcome from Plan 11-01
    - Makefile — `make build`, `make test`, `make test-rust`, `make test-java`, and lint targets; `make test-java` builds the shim and runs both JNA and Panama
    - shim/Cargo.toml and shim/src/lib.rs — final cdylib and unchanged C-ABI context
    - compat_test.go — Go compile-time public surface gate
    - java/jna and java/panama build/test configuration — existing Java smoke suites and built-shim loading path
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md — D-02, D-08, and D-09 boundaries
  </read_first>
  <action>Run `make build` after the migration, resolve the same platform-specific debug library, and repeat Plan 11-01's actual-library inspection procedure using `nm -gjU` on macOS, `nm -D --defined-only` on Linux, or `llvm-nm` then `dumpbin /exports` on Windows. Normalize and sort only `chroma_*` names into `11-abi-after.exports`. When both lists exist, run `diff -u 11-abi-before.exports 11-abi-after.exports`, require zero output, and write `11-abi-export-comparison.md` with both paths, commands, hashes, timestamp, and `RESULT: identical`. If either inspection was unavailable, write `RESULT: pending supported inspector` and include the exact prior/current command attempts, errors, and pre-migration SHA; do not call that a pass or silently omit it. This is a blocking UPG-03 ABI gate: Plan 11-04 must execute the same before/after comparison on a supported runner before phase closure. Then run the repository-confirmed fresh-data checks `make test`, `make test-rust`, and `make test-java`. Explicitly record in the plan summary that `make test-java` rebuilds/uses the native shim and validates both JNA and Panama; do not describe it as one backend. Run `make lint` when its required local tools are available; if any tool is absent or fails before examining Phase 11 code, record the exact command/output and leave the CI workflow-lint result as the final evidence gate rather than treating tool absence as a migration defect. Do not modify public C declarations, Go facade/runtime APIs, JNA/Panama declarations, Java core types, or persisted-data fixtures. Addresses review concerns: artifact-level export comparison, confirmed Make targets, Java dual-backend coverage, and diagnostic lint availability.</action>
  <verify>
    <automated>test -s .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-export-comparison.md &amp;&amp; (cmp -s .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-before.exports .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-after.exports || rg -q '^RESULT: pending supported inspector$' .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-export-comparison.md) &amp;&amp; make test &amp;&amp; make test-rust &amp;&amp; make test-java</automated>
  </verify>
  <acceptance_criteria>Where inspection is available, before/after sorted native `chroma_*` sets are byte-identical and the comparison record proves it; otherwise unavailability is explicit, attributable, and leaves a mandatory supported-runner comparison for Plan 11-04. `make test`, `make test-rust`, and `make test-java` pass with the migrated/rebuilt shim, and the recorded evidence names both JNA and Panama. Any lint-tool limitation is exact diagnostic evidence, not a false claim of either success or migration failure.</acceptance_criteria>
  <done>Fresh-data binding evidence is artifact-based and accurately bounded. The ABI result is either proven identical or remains a blocking Plan 11-04 supported-runner gate; remote CI remains that plan's independent cross-platform gate.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contributor guidance to source build | Published tool versions determine whether a contributor can reproduce the reviewed graph. |
| Rebuilt native library to language bindings | Go purego, JNA, and Panama share one dynamic ABI. |
| Fresh-data test result to release claim | New data does not establish existing-data upgrade or maintenance compatibility. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-07 | Tampering | README toolchain contract | mitigate | D-06 exact roles: Rust 1.88 source build, Rust 1.93.1 CI/release, protoc 31.1, no tools for prebuilt consumers. |
| T-11-08 | Tampering / denial of service | Native C ABI and bindings | mitigate | Compare actual exports, run `make test`, `make test-rust`, and dual-backend `make test-java` against a rebuilt shim. |
| T-11-09 | Repudiation | Compatibility claim | mitigate | Record fresh-data-only result and preserve Phase 12 ownership of persisted-data/mutation/reopen/maintenance proof. |
| T-11-SC | Tampering | Dependency build inputs | mitigate | All test/build commands consume Plan 11-01's committed locked graph; no package install or resolver mutation occurs. |
</threat_model>

<verification>
Completion requires real native artifact comparison when the platform supports it plus all three local suites. Tool unavailability and lint limits must be recorded precisely; neither becomes an invented compatibility result.
</verification>

<success_criteria>
- README and AGENTS.md express the exact hybrid toolchain contract and Phase 12/13 boundaries without altering workflows unnecessarily.
- The built native `chroma_*` set is compared before/after; an unavailable local comparison is transparently recorded as a blocking Plan 11-04 supported-runner gate.
- `make test-java` is documented and evidenced as testing both JNA and Panama against the rebuilt shim.
- Fresh-data local results do not claim persisted-data upgrade, mutation/reopen, or maintenance compatibility.
</success_criteria>

<output>
Create `.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-03-SUMMARY.md` when done.
</output>
