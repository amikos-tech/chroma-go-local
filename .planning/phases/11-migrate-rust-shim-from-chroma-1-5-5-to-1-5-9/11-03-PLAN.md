---
phase: 11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9
plan: "03"
type: execute
wave: 3
depends_on:
  - 11-02
files_modified:
  - README.md
  - AGENTS.md
autonomous: true
requirements:
  - UPG-03
  - UPG-04
user_setup: []
must_haves:
  truths:
    - "Source-build contributors know Rust 1.88.0 and protoc 31.1 are required, while CI and releases retain Rust 1.93.1 (D-06)."
    - "Prebuilt native-library consumers are not told to install Rust or protoc (D-06)."
    - "The public C, Go, JNA, and Panama contracts remain stable and are exercised against fresh data; additive 1.5.9 APIs remain deferred (D-01, D-02)."
    - "The project does not mistake fresh-data test success for compatibility with existing 1.5.5 data (D-08, D-09)."
  artifacts:
    - path: README.md
      provides: "Accurate source-build, CI/release, prebuilt-consumer, deferred-API, and Phase 12 compatibility guidance for library users."
      contains: "Rust 1.88.0"
    - path: AGENTS.md
      provides: "Contributor-facing hybrid source-build and CI/release toolchain contract."
      contains: "Rust 1.88.0"
  key_links:
    - from: README.md
      to: .github/workflows/ci.yml
      via: "documented CI/release Rust 1.93.1 and protoc 31.1 pins"
      pattern: "1\\.93\\.1|31\\.1"
    - from: compat_test.go
      to: "Go public API"
      via: "compile-time public-surface gate exercised by Go tests"
      pattern: "var _ chroma\\."
    - from: "java/jna and java/panama tests"
      to: "existing Rust C ABI"
      via: "smoke tests using the built debug shim"
      pattern: "chroma_"
---

<objective>
State the actual 1.5.9 build contract, explicitly defer additive upstream APIs, and validate the stable bindings against fresh data.

Purpose: A dependency upgrade is releasable only when contributor guidance, ABI/API compatibility evidence, and the scope boundary all agree.
Output: Corrected README.md and AGENTS.md guidance plus recorded local fresh-data validation evidence, without any C, Go, JNA, or Panama API expansion.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-RESEARCH.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-PATTERNS.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-02-SUMMARY.md
</context>

## Source Coverage Audit

| SOURCE | ID | Feature / requirement | Plan | Status | Notes |
|--------|----|-----------------------|------|--------|-------|
| GOAL | — | Upgrade the Rust shim to Chroma 1.5.9 while preserving C, Go, JNA, and Panama contracts | 11-01 to 11-04 | COVERED | Dependency graph, local call adaptation, local binding validation, and remote PR evidence are separated by dependency. |
| REQ | UPG-01 | Nine direct Chroma dependencies and lockfile use the 1.5.9 graph | 11-01 | COVERED | Includes targeted fastrace 0.7.8 reconciliation and lockfile review. |
| REQ | UPG-02 | Adapt `Frontend::delete` with local-mode semantics and unchanged FFI behavior | 11-02 | COVERED | Uses `String::new()` and a real embedded deletion regression. |
| REQ | UPG-03 | Preserve exported C and public binding APIs; explicitly disposition additive APIs | 11-02, 11-03, 11-04 | COVERED | C declaration protection, deferred-API documentation, local binding tests, and exact-SHA PR evidence. |
| REQ | UPG-04 | Toolchain guidance matches the locked 1.5.9 graph | 11-01, 11-03 | COVERED | Manifest MSRV plus contributor/CI/release documentation. |
| RESEARCH | — | Constrained resolver update, fastrace conflict, duplicate review, and `--locked` graph validation | 11-01, 11-02 | COVERED | No bare `cargo update`; Rust 1.88.0 check occurs after the source adaptation. |
| RESEARCH | — | Default-compatible YAML/schema parsing with no typed API expansion | 11-03 | COVERED | Existing Go and Java fresh-data test suites retain config parsing coverage; no new config surface is added. |
| RESEARCH | — | Fresh-data evidence is not persisted-data upgrade proof | 11-03, 11-04 | COVERED | Phase 12 remains the compatibility release gate even after the cross-platform PR matrix passes. |
| CONTEXT | D-01 | Defer UUID lookup, fork_count, bounded-WAL read level, sparse selection, and MaxScore controls | 11-03 | COVERED | All five capabilities are named as deferred to Phase 13. |
| CONTEXT | D-02 | Keep raw parsing default-compatible; add no public C/Go/JNA/Panama API | 11-03 | COVERED | Tests and source-surface inspection protect the unchanged API. |
| CONTEXT | D-03 | Use `String::new()` as private local delete region | 11-02 | COVERED | Implemented at the sole upstream call site. |
| CONTEXT | D-04 | Real embedded delete regression verifies deletion and survivor | 11-02 | COVERED | Public Go → C ABI → Rust frontend path. |
| CONTEXT | D-05 | Move all nine tags together; targeted fastrace update only | 11-01 | COVERED | Generated lockfile inspected as a dependency migration. |
| CONTEXT | D-06 | Rust 1.88.0 MSRV, Rust 1.93.1 CI/release, protoc 31.1 | 11-01, 11-03 | COVERED | Manifest, README, and AGENTS.md align with unchanged exact workflow pins. |
| CONTEXT | D-07 | Revalidate Rust 1.88.0 locked all-targets check after material lockfile change | 11-02 | COVERED | Runs after the source adaptation makes the graph compilable. |
| CONTEXT | D-08 | Fresh-data tests and normal Linux/macOS/Windows PR matrix only | 11-03, 11-04 | COVERED | Local fresh-data tests are followed by a human-authorized PR and a blocking, recorded normal CI result for the exact migrated commit. |
| CONTEXT | D-09 | Phase 12 owns persisted-data compatibility, maintenance, and binding evidence | 11-03 | COVERED | README and final verification name Phase 12 as release gate. |

<interfaces>
Existing public-surface evidence to retain, not edit:

```go
// compat_test.go compiles every public Go symbol, including:
var _ chroma.EmbeddedDeleteRecordsRequest
var _ chroma.EmbeddedDeleteRecordsResponse
```

```rust
// The two retained C entrypoints are exercised indirectly by Go/JNA/Panama:
chroma_embedded_delete_records
chroma_embedded_delete_records_with_response
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Align contributor documentation with the locked toolchain and feature boundary</name>
  <files>README.md, AGENTS.md</files>
  <read_first>
    - README.md:10-16 — current top-level requirements
    - README.md:85-103 — Windows source-build prerequisites
    - README.md:629-657 — test commands and current fresh-data CI description
    - README.md:695-702 — troubleshooting references to protoc
    - AGENTS.md:9-19 — repository-level build requirements that currently advertise an obsolete Rust floor
    - .github/workflows/ci.yml:60-150, 175-250, and 293-370 — retained Rust 1.93.1/protoc 31.1 three-OS PR job contract
    - .github/workflows/release.yml:49-114 — retained Rust 1.93.1/protoc 31.1 release build contract
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md — D-01, D-02, D-06, D-08, and D-09 are locked
    - .planning/spikes/002-chroma-1-5-9-toolchain-floor/README.md — measured Rust/protoc evidence
  </read_first>
  <action>
    Per D-06, edit both `README.md` and `AGENTS.md` to replace every stale `Rust 1.70+` source-build claim with the same hybrid contract: Rust 1.88.0 is the source-build MSRV for the committed lockfile; CI and releases use the exact Rust 1.93.1 pin; source builds require protoc 31.1; and consumers of prebuilt native libraries need neither Rust nor protoc. In `README.md`, update the Windows setup and troubleshooting references from `31.x`/Chroma 1.5.5 wording to the exact `protoc` 31.1 Chroma 1.5.9 contract. In `AGENTS.md`, retain its Java and lint requirements while making its Requirements section distinguish source-build tools from the already-pinned CI/release compiler and prebuilt-consumer path. Verify `.github/workflows/ci.yml` and `.github/workflows/release.yml` retain their existing exact Rust 1.93.1 and protoc 31.1 pins; do not change those workflows unless their actual pins no longer match D-06. Per D-01 and D-02, add a concise `README.md` migration-scope note that no new C, Go, JNA, or Panama APIs are exposed here and that UUID collection lookup, `fork_count`, `ReadLevel::IndexAndBoundedWal`, sparse-index `Wand`/`MaxScore` selection, and MaxScore tenant controls are deferred to Phase 13. Per D-08 and D-09, state that fresh-data local tests and the normal Linux/macOS/Windows PR matrix validate this migration but do not prove persisted 1.5.5-data upgrade, mutation/reopen, maintenance, or cross-version compatibility; Phase 12 remains that release gate.
  </action>
  <verify>
    <automated>rg -n 'Rust 1\.88\.0|Rust 1\.93\.1|protoc.?31\.1|prebuilt native|Phase 12|Phase 13|fork_count|IndexAndBoundedWal|MaxScore' README.md &amp;&amp; rg -n 'Rust 1\.88\.0|Rust 1\.93\.1|protoc.?31\.1|prebuilt native' AGENTS.md &amp;&amp; ! rg -n 'Rust 1\.70\+' README.md AGENTS.md &amp;&amp; ! rg -n 'protoc 31\.x|Chroma `1\.5\.5` toolchain' README.md &amp;&amp; rg -n 'toolchain:[[:space:]]*1\.93\.1|toolchain: 1\.93\.1' .github/workflows/ci.yml .github/workflows/release.yml &amp;&amp; rg -n 'PROTOC_VERSION:[[:space:]]*"31\.1"' .github/workflows/ci.yml .github/workflows/release.yml</automated>
  </verify>
  <acceptance_criteria>Both README.md and AGENTS.md name Rust 1.88.0, exact Rust 1.93.1 CI/release, and protoc 31.1 with their distinct roles; neither file retains `Rust 1.70+` or any other source-build guidance below the locked MSRV; both exclude prebuilt consumers from source tools; README.md lists all deferred additive APIs and draws the Phase 11 fresh-data versus Phase 12 persisted-data boundary; and both workflows retain their exact 1.93.1/31.1 pins.</acceptance_criteria>
  <done>Contributor and repository-agent guidance exactly match D-06 and scope decisions, while already-correct CI/release workflow pins remain unchanged.</done>
</task>

<task type="auto">
  <name>Task 2: Prove fresh-data binding compatibility and preserve the public contract boundary</name>
  <files>README.md</files>
  <read_first>
    - AGENTS.md — required Make targets, no-cgo constraint, and validation contract
    - compat_test.go:1-90 — compile-time Go public API surface gate
    - shim/src/lib.rs:4190-4231 — stable exported embedded-delete C symbols
    - shim/tests/ffi_integration.rs:1-100 — direct FFI smoke-test pattern
    - Makefile:131-174 — `test-go`, `test-rust`, `test-java`, `test-all`, and lint targets
    - .github/workflows/ci.yml:124-150, 247-265, and 365-383 — existing Linux/macOS/Windows fresh-data Go/Rust/JNA/Panama coverage
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md — D-02, D-08, and D-09 are locked
  </read_first>
  <action>
    Per D-02, do not edit public C declarations, Go facade/runtime API definitions, JNA declarations, Panama declarations, or Java core API types. Build and test the migrated shim with the repository Make targets: run `make test-go`, `make test-rust`, `make test-java`, and `make lint`; use `make test-all` only as the aggregate convenience command when Gradle is available, noting that its Java section may be skipped by its existing guard. Confirm the checked public API guard compiles, Rust FFI integration tests pass, and the JNA and Panama smoke suites use the built native library, including their existing default-compatible YAML/config parsing coverage. Review the normal PR CI configuration only to identify the expected three-OS fresh-data jobs and retained Rust 1.93.1/protoc 31.1 pins; source review and local commands are not evidence of a green PR matrix. Record the local validation results accurately: they prove fresh-data compatibility and stable binding paths, while D-09 keeps persisted 1.5.5-data fixtures, upgrade/mutation/reopen, maintenance behavior, and cross-version proof exclusively in Phase 12.
  </action>
  <verify>
    <automated>make test-go &amp;&amp; make test-rust &amp;&amp; make test-java &amp;&amp; make lint &amp;&amp; rg -n 'chroma_embedded_delete_records(_with_response)?' shim/src/lib.rs &amp;&amp; rg -n 'EmbeddedDeleteRecords(Request|Response)' compat_test.go</automated>
  </verify>
  <acceptance_criteria>Go compatibility compilation, Rust FFI tests, existing Go/Java YAML-config coverage, JNA smoke tests, Panama smoke tests, and lint all pass using the migrated shim; the existing delete C exports are present; and no public C/Go/JNA/Panama API file is changed. A workflow-source review identifies the expected three-OS jobs but does not claim that remote CI passed.</acceptance_criteria>
  <done>Local fresh-data compatibility evidence is complete and accurately bounded; the actual normal PR CI result remains the blocking final gate in Plan 11-04, and persisted-data upgrade evidence remains Phase 12 work.</done>
</task>

</tasks>

<threat_model>
## ASVS-L1 Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Contributor documentation to source build | Published versions determine which compiler and protobuf generator can reproduce the reviewed lockfile. |
| Public binding APIs to native shim | Go purego, JNA, and Panama invoke an existing C ABI; accidental API/ABI expansion would affect every consumer. |
| Fresh-data tests to release claim | A passing new database differs from proof that an existing 1.5.5 data directory can upgrade safely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | ASVS-L1 mitigation plan |
|-----------|----------|-----------|-------------|--------------------------|
| T-11-07 | Tampering | README.md and AGENTS.md toolchain guidance | mitigate | Document exact Rust 1.88.0 source-build MSRV, exact Rust 1.93.1 CI/release pin, protoc 31.1, and the prebuilt-consumer exception in both guidance files; cross-check the unchanged pinned workflows. |
| T-11-08 | Tampering | C/Go/JNA/Panama compatibility contract | mitigate | Do not edit public binding surfaces; compile `compat_test.go`, run Rust FFI plus JNA/Panama smoke tests, and retain existing C export names/signatures. |
| T-11-09 | Repudiation | migration compatibility claim | mitigate | Block completion on the exact migrated commit's completed/successful normal PR CI matrix, record its URL/SHA/nine job results, and explicitly assign persisted-data compatibility, mutation/reopen, and maintenance proof to Phase 12. |
| T-11-SC | Tampering | dependency build inputs | mitigate | All Rust tests and lint run against the committed `--locked` Cargo graph established in Plans 11-01 and 11-02. |
</threat_model>

<verification>
Run the local task commands. Plan 11-04 then pauses for the user to push the exact committed branch and create or update the normal PR before automated SHA-matched CI verification records the evidence. Workflow-source review and local commands do not satisfy that final D-08 gate.
</verification>

<success_criteria>
- README.md and AGENTS.md accurately describe the 1.5.9 source-build, CI/release, and prebuilt-consumer toolchain contract with no stale Rust 1.70+ claim.
- Additive upstream features are explicitly deferred, with no public C, Go, JNA, or Panama API expansion.
- Fresh-data Go, Rust, JNA, and Panama paths pass through existing Make targets; Plan 11-04 records the exact migrated commit's completed/successful normal PR CI matrix results for all Linux, macOS, and Windows jobs.
- The documentation and validation record reserve persisted-data compatibility evidence for Phase 12.
</success_criteria>

<output>
Create `.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-03-SUMMARY.md` when done.
</output>
