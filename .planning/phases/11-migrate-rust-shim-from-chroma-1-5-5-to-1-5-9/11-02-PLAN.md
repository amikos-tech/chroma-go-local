---
phase: 11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9
plan: "02"
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - shim/src/lib.rs
  - internal/runtime/embedded_integration_edge_test.go
autonomous: true
requirements:
  - UPG-02
user_setup: []
must_haves:
  truths:
    - "Embedded record deletion still works through the existing Go-to-C-to-Rust FFI path after Chroma's delete signature changes (D-03)."
    - "A delete regression proves that the targeted record is gone while a different record remains retrievable (D-04)."
    - "The stable delete C ABI accepts the same arguments and returns the same types as before."
    - "A material lockfile change is revalidated with `cargo +1.88.0 check --all-targets --locked` (D-07)."
  artifacts:
    - path: shim/src/lib.rs
      provides: "Local embedded `Frontend::delete(request, String::new())` adaptation behind existing C exports."
      contains: "frontend.delete(request, String::new()).await"
    - path: internal/runtime/embedded_integration_edge_test.go
      provides: "Real-runtime delete regression asserting both deletion and survivor retrieval."
      contains: "DeleteRecords"
  key_links:
    - from: internal/runtime/embedded_integration_edge_test.go
      to: shim/src/lib.rs
      via: "Embedded.DeleteRecords -> chroma_embedded_delete_records_with_response -> run_embedded_delete_records"
      pattern: "chroma_embedded_delete_records_with_response"
---

<objective>
Adapt the one changed upstream Rust method call while keeping the C ABI and all language-binding inputs unchanged, then prove real embedded deletion behavior.

Purpose: Chroma 1.5.9 adds a Rust-only `region` argument; this plan absorbs it as private local runtime state rather than expanding public APIs.
Output: A one-line Rust adaptation and an end-to-end Go embedded delete regression.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-RESEARCH.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-PATTERNS.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-01-SUMMARY.md
</context>

<interfaces>
From `shim/src/lib.rs` — retained C ABI contracts; their symbol names, parameters, and return types must not change:

```rust
#[no_mangle]
pub unsafe extern "C" fn chroma_embedded_delete_records(
    handle: *mut c_void,
    request_json: *const c_char,
) -> i32;

#[no_mangle]
pub unsafe extern "C" fn chroma_embedded_delete_records_with_response(
    handle: *mut c_void,
    request_json: *const c_char,
) -> *mut c_char;
```

From `internal/runtime/embedded.go` — public Go calls that must remain unchanged:

```go
func (e *Embedded) DeleteRecords(request EmbeddedDeleteRecordsRequest) error
func (e *Embedded) DeleteRecordsWithResponse(request EmbeddedDeleteRecordsRequest) (*EmbeddedDeleteRecordsResponse, error)
func (e *Embedded) GetRecords(request EmbeddedGetRecordsRequest) (*EmbeddedGetRecordsResponse, error)
```
</interfaces>

<tasks>

<task type="auto">
  <name>Task 1: Pass the locked local region through Chroma 1.5.9 without altering the FFI contract</name>
  <files>shim/src/lib.rs</files>
  <read_first>
    - shim/src/lib.rs:2577-2624 — `run_embedded_delete_records`, its parse/lock/runtime/error handling, and the changed upstream call site
    - shim/src/lib.rs:4190-4231 — existing `chroma_embedded_delete_records` and `chroma_embedded_delete_records_with_response` C exports
    - internal/runtime/embedded.go:1164-1204 — stable public Go delete API and its binding to the response C export
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md — D-03, D-04, and D-07 are locked
    - .planning/spikes/001-chroma-1-5-9-lock-resolution/README.md — source adaptation required before the targeted graph compiles
  </read_first>
  <action>
    Per D-03, make the smallest internal change at the final async call in `run_embedded_delete_records`: call `frontend.delete(request, String::new()).await`. The empty `String` is the local embedded-mode region selected by the validated upstream precedent; it is telemetry-only private shim state, not a configuration value or caller input. Preserve payload parsing, request validation, frontend lock-poisoning behavior, runtime execution, error slot text, error codes, and response serialization. Do not change either `#[no_mangle] extern "C"` delete export, do not add a region parameter anywhere outside this Rust call, and do not introduce cgo or public C/Go/JNA/Panama API changes. Per D-07, after this source edit and the material lockfile migration, run the required MSRV locked all-targets check before adding the end-to-end regression.
  </action>
  <verify>
    <automated>cargo +1.88.0 check --manifest-path shim/Cargo.toml --all-targets --locked &amp;&amp; rg -n 'frontend\.delete\(request, String::new\(\)\)\.await' shim/src/lib.rs &amp;&amp; git diff --check -- shim/src/lib.rs</automated>
  </verify>
  <acceptance_criteria>The migrated shim compiles with Rust 1.88.0 and a locked graph, and neither exported C delete function changes name, parameter list, or return type.</acceptance_criteria>
  <done>Chroma's Rust-only delete signature change is absorbed behind the unchanged stable FFI contract.</done>
</task>

<task type="auto">
  <name>Task 2: Add the real embedded delete regression against the migrated shim</name>
  <files>internal/runtime/embedded_integration_edge_test.go</files>
  <read_first>
    - internal/runtime/embedded_integration_edge_test.go:13-34 — `newEmbeddedForIntegrationTest` lifecycle helper
    - internal/runtime/embedded_integration_edge_test.go:146-195 and 249-320 — focused delete tests and the established eventual-consistency assertions
    - internal/runtime/embedded.go:262-285 and 1164-1204 — stable `GetRecords`, `DeleteRecords`, and FFI-call behavior
    - shim/src/lib.rs:2577-2624 and 4190-4231 — adapted Rust runner and unchanged C delete exports
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md — D-03 and D-04 are locked
  </read_first>
  <action>
    Per D-04, add one focused test adjacent to the existing embedded delete cases. Start an embedded instance with `newEmbeddedForIntegrationTest`, create a uniquely named database and collection, seed two records with deterministic distinct IDs and embeddings, then delete exactly the target ID through the existing `Embedded.DeleteRecords` API. Use the existing five-second/100-millisecond `require.Eventually` convergence policy; do not use a sleep. Once the record count reaches one, call `GetRecords` with both IDs (or equivalent public retrieval assertions) and assert the target is absent and the survivor is present. Keep the test in the `runtime` package and do not add a test-only C, Go, JNA, or Panama API.
  </action>
  <verify>
    <automated>make test-go &amp;&amp; git diff --check -- internal/runtime/embedded_integration_edge_test.go</automated>
  </verify>
  <acceptance_criteria>The new real-runtime test passes, reports one remaining record, omits the deleted ID, and retains the survivor ID through the unchanged Go/FFI boundary.</acceptance_criteria>
  <done>A focused regression proves deletion and survivor preservation through the stable public API and native shim.</done>
</task>

</tasks>

<threat_model>
## ASVS-L1 Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go API to C ABI | Public Go calls serialize requests into existing native symbols; callers must not acquire a new region parameter. |
| C ABI to Chroma Rust frontend | The shim translates fixed exported signatures to an upstream method whose internal signature changed. |
| Committed lockfile to Rust compiler | The code is valid only against the reviewed, locked 1.5.9 graph. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | ASVS-L1 mitigation plan |
|-----------|----------|-----------|-------------|--------------------------|
| T-11-04 | Tampering | `run_embedded_delete_records` | mitigate | Use exactly `String::new()` per D-03, preserve parsing/error handling, and prove delete plus survivor behavior through the public Go API. |
| T-11-05 | Elevation of privilege | `chroma_embedded_delete_records*` | mitigate | Preserve the existing two exported C function signatures; no caller-controlled region, cgo bridge, or new binding surface is introduced. |
| T-11-06 | Tampering | `shim/Cargo.lock` and Rust source | mitigate | Run `cargo +1.88.0 check --all-targets --locked` after the source adaptation so the compiled source cannot silently alter the reviewed dependency graph. |
| T-11-SC | Tampering | Cargo resolution | mitigate | Retain Plan 11-01's exact lockfile and verify it with `--locked`; this plan does not add packages or run unconstrained resolution. |
</threat_model>

<verification>
Run `cargo +1.88.0 check --manifest-path shim/Cargo.toml --all-targets --locked` and `make test-go`. Inspect the staged Rust diff to confirm the call-site substitution is the sole production-code FFI change and that the two public C exports remain unchanged.
</verification>

<success_criteria>
- The local embedded delete call compiles against `Frontend::delete(request, region)` with `String::new()`.
- The existing C exports and public Go delete methods retain their names, input shapes, and return behavior.
- A real embedded regression proves deleted records are absent and unrelated records remain available.
- The migrated graph passes the mandated Rust 1.88.0 locked all-targets check.
</success_criteria>

<output>
Create `.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-02-SUMMARY.md` when done.
</output>
