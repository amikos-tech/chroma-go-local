---
phase: 11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9
plan: "02"
type: execute
wave: 2
depends_on:
  - 11-01
files_modified:
  - shim/src/lib.rs
  - internal/runtime/embedded_integration_edge_test.go
autonomous: true
requirements:
  - UPG-02
  - UPG-03
must_haves:
  truths:
    - "Embedded deletion uses an empty private region for Chroma 1.5.9 and does not expose a new caller input (D-03)."
    - "Deleting one record through the public Go API leaves a distinct record retrievable through Go → existing C ABI → Rust (D-04)."
    - "Unexpected upstream compile drift is either resolved through a documented minimal private Rule-1 correction or escalated before any ABI/public/data change."
  artifacts:
    - path: shim/src/lib.rs
      provides: "Private `Frontend::delete(request, String::new())` adaptation behind stable exports, with a local-mode telemetry rationale."
      contains: "frontend.delete(request, String::new())"
    - path: internal/runtime/embedded_integration_edge_test.go
      provides: "Focused real-runtime delete/survivor regression."
      contains: "TestEmbeddedDeleteByIDPreservesSurvivor"
  key_links:
    - from: internal/runtime/embedded_integration_edge_test.go
      to: shim/src/lib.rs
      via: "public Embedded.DeleteRecords through existing chroma_embedded_delete_records export"
      pattern: "DeleteRecords"
    - from: shim/src/lib.rs
      to: "Chroma Frontend::delete"
      via: "private empty region argument"
      pattern: "delete\\(request, String::new\\(\\)\\)"
---

<objective>
Adapt the only known upstream Rust signature change privately and prove the unchanged public embedded-delete path removes only its target record.

Purpose: This preserves the C ABI and all Go/JNA/Panama callers while compiling against Chroma 1.5.9.
Output: One internal Rust call-site adaptation and one focused Go-to-native real-runtime regression.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-RESEARCH.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-VALIDATION.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-PATTERNS.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-01-SUMMARY.md
</context>

<interfaces>
From `shim/src/lib.rs`, retain exactly these existing entrypoints and shapes:

```rust
pub unsafe extern "C" fn chroma_embedded_delete_records(handle: *mut c_void, request_json: *const c_char) -> i32;
pub unsafe extern "C" fn chroma_embedded_delete_records_with_response(handle: *mut c_void, request_json: *const c_char) -> *mut c_char;
```

From `internal/runtime/embedded.go`, use the existing public `Embedded.DeleteRecords(EmbeddedDeleteRecordsRequest)` and `Embedded.GetRecords` calls; do not add a region option or any binding surface.
</interfaces>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add the public embedded-delete survivor regression before adapting Rust</name>
  <files>internal/runtime/embedded_integration_edge_test.go</files>
  <read_first>
    - internal/runtime/embedded_integration_edge_test.go — `newEmbeddedForIntegrationTest`, existing delete cases, unique-name construction, and `require.Eventually` convergence policy
    - internal/runtime/embedded.go — existing `DeleteRecords` and `GetRecords` request/response contracts
    - compat_test.go — compile-time public Go API compatibility guard
    - shim/src/lib.rs — `run_embedded_delete_records` and the two current exported delete functions, which must stay unchanged
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md — D-03 and D-04 are locked
  </read_first>
  <behavior>
    - Given a fresh embedded database/collection with deterministic IDs `keep` and `delete`, deleting only `delete` eventually yields exactly one record.
    - Public retrieval shows `delete` absent and `keep` present, proving both removal and survivor preservation through the native shim.
    - The test has no fake FFI handle, no new C symbol, no fixed sleep, and its isolated database/collection names cannot collide with parallel tests.
  </behavior>
  <action>Per D-04, add `TestEmbeddedDeleteByIDPreservesSurvivor` adjacent to existing embedded delete integration cases. Reuse `newEmbeddedForIntegrationTest`; create uniquely named database and collection values using the established test pattern; add two records with distinct IDs, documents, and valid deterministic embeddings; call only the existing public `Embedded.DeleteRecords` with the target ID. Use the existing five-second/100-millisecond `require.Eventually` policy to wait for the count and retrieval state, then assert count one, target ID absent, and survivor ID present. Start by running the focused test after it is written; it should demonstrate the old call does not build against the updated graph, then become green after Task 2. Use the repository-confirmed `make test` for full Go coverage after the focused invocation; do not assume a `make test-go` target. Addresses review concern: behavior evidence replaces formatting-sensitive source matching.</action>
  <verify>
    <automated>test -z "$(gofmt -d internal/runtime/embedded_integration_edge_test.go)" &amp;&amp; rg -q '^func TestEmbeddedDeleteByIDPreservesSurvivor\(' internal/runtime/embedded_integration_edge_test.go &amp;&amp; ! cargo +1.88.0 check --manifest-path shim/Cargo.toml --all-targets --locked</automated>
  </verify>
  <acceptance_criteria>The focused test compiles syntactically, contains the public deletion-and-survivor assertions, and the expected pre-adaptation Rust compile failure is observed before Task 2 turns the behavior green.</acceptance_criteria>
  <done>A RED regression describes the required observable behavior before the production call-site fix is finalized.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Apply the private delete-region correction and enforce the Rule-1 boundary</name>
  <files>shim/src/lib.rs</files>
  <read_first>
    - shim/src/lib.rs — complete `run_embedded_delete_records` error, lock, and runtime-blocking flow plus the two C exports
    - shim/Cargo.toml and shim/Cargo.lock — Plan 11-01's final locked 1.5.9 graph
    - internal/runtime/embedded_integration_edge_test.go — Task 1 behavior contract
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md — D-03 and D-07 are locked; D-01/D-02 defer API expansion
    - /Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md — Rule-1 versus Rule-4 deviation procedure
  </read_first>
  <behavior>
    - `run_embedded_delete_records` supplies `String::new()` as the second private local-mode argument to `Frontend::delete` and documents that the empty region is local embedded telemetry state, intentionally absent from C, Go, JNA, and Panama.
    - Existing JSON parsing, error codes, lock-poison behavior, and both exported C function signatures retain their current behavior.
    - The focused delete/survivor regression and the final Rust 1.88.0 locked all-targets compile pass.
  </behavior>
  <action>Per D-03, change only the private frontend invocation in `run_embedded_delete_records` to call `frontend.delete(request, String::new()).await`; add a concise adjacent Rust comment that the empty region is local embedded telemetry state and is intentionally not exposed through C, Go, JNA, or Panama. Retain the current parse, request validation, mutex, runtime, error-code, and error-message flow. Do not change either `chroma_embedded_delete_records*` export, C parameter/return types, Go facade/runtime declarations, JNA/Panama declarations, Java APIs, YAML/raw-schema parsing, or persisted-data behavior. Immediately run `cargo +1.88.0 check --manifest-path shim/Cargo.toml --all-targets --locked` and the focused Go regression. If Chroma 1.5.9 exposes an unexpected compile error, a Rule-1 correction is authorized only when it is minimal and private to the Rust shim, preserves every existing C ABI/public Go/JNA/Panama API and data format, is supported by focused compile/test evidence, and is recorded in `11-02-SUMMARY.md` with the upstream error, affected private identifier, exact correction, and rationale. Any proposed C ABI/public API/data-format/persisted-data change, new typed wrapper, or non-local architectural change is Rule-4: stop, leave it unimplemented, and request a decision. Addresses review concern: source-adaptation fallback is explicit and compatibility-preserving rather than generic deviation handling.</action>
  <verify>
    <automated>cargo +1.88.0 check --manifest-path shim/Cargo.toml --all-targets --locked &amp;&amp; make build &amp;&amp; go test ./internal/runtime -run '^TestEmbeddedDeleteByIDPreservesSurvivor$' -count=1 &amp;&amp; git diff --check -- shim/src/lib.rs internal/runtime/embedded_integration_edge_test.go</automated>
  </verify>
  <acceptance_criteria>The build compiles against the 1.5.9 two-argument delete signature; a concise adjacent comment documents the empty region as private local telemetry state; the public regression passes; no exported symbol or public Go/JNA/Panama declaration changed; and any additional private Rule-1 correction has focused proof and a summary rationale, while Rule-4 changes are escalated instead of implemented.</acceptance_criteria>
  <done>The known upstream signature drift is safely contained behind the existing ABI with an explicit boundary for any discovered drift.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Go public API to C ABI | Existing callers serialize delete requests without a region parameter. |
| C ABI to Chroma frontend | The private Rust shim adapts a changed upstream method signature. |
| Locked dependency graph to private source correction | Compiler errors can pressure unplanned surface changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-03 | Tampering | `run_embedded_delete_records` | mitigate | D-03 exact empty-region call, focused public regression, and retained parse/error path. |
| T-11-05 | Elevation of privilege | Existing C delete exports | mitigate | No caller-controlled region and no C/Go/JNA/Panama declaration change. |
| T-11-06 | Denial of service | Unexpected upstream signature drift | mitigate | Rust 1.88 locked compile; Rule-1 allows only evidence-backed private fixes and Rule-4 stops surface/data changes. |
| T-11-SC | Tampering | Cargo graph | mitigate | Consume Plan 11-01's final `--locked` resolution; no resolver command or new package is introduced. |
</threat_model>

<verification>
The focused public test and locked Rust compile are required after the change. A successful source grep is not proof of API or ABI preservation; Plan 11-03 compares built exports and exercises Go/JNA/Panama.
</verification>

<success_criteria>
- `Frontend::delete` receives the private `String::new()` region exactly as D-03 requires, with its local-only telemetry semantics documented beside the call.
- The test proves a requested deletion does not remove a survivor through the real public/native path.
- No ABI, public binding, raw-schema, or persisted-data compatibility work leaks into Phase 11.
- Additional compile drift follows the stated Rule-1/Rule-4 decision boundary.
</success_criteria>

<output>
Create `.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-02-SUMMARY.md` when done.
</output>
