---
phase: 11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9
plan: "04"
type: execute
wave: 4
depends_on:
  - 11-03
files_modified:
  - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-before.exports
  - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-after.exports
  - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-export-comparison.md
  - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-04-SUMMARY.md
autonomous: false
requirements:
  - UPG-03
must_haves:
  truths:
    - "An open, non-draft normal PR for the exact migrated SHA has completed successful fresh-data CI evidence across the workflow's complete actual returned job set (D-08)."
    - "The evidence records PR state, successful run conclusion, and actual job metadata rather than fixed display names; it does not claim persisted-data compatibility (D-08, D-09)."
    - "The native before/after chroma_* export comparison is `RESULT: identical`; any locally unavailable inspection is re-run on a supported inspector before UPG-03 can close."
    - "Lack of authenticated GitHub CLI or network access uses authorized manual evidence rather than being mislabeled as a technical migration failure."
  artifacts:
    - path: .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-04-SUMMARY.md
      provides: "Exact SHA/open-PR/successful-run evidence with timestamp and complete dynamically discovered job outcomes, or authorized manual equivalent."
      contains: "PR CI evidence"
    - path: .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-export-comparison.md
      provides: "Actual before/after native ABI comparison with `RESULT: identical`."
      contains: "RESULT: identical"
  key_links:
    - from: "local migrated SHA"
      to: "open normal PR and CI run"
      via: "head SHA equality checks"
      pattern: "headSha"
    - from: "gh run view JSON jobs"
      to: 11-04-SUMMARY.md
      via: "actual returned job names, statuses, and conclusions"
      pattern: "jobs"
    - from: "pre-migration SHA recorded by Plan 11-01"
      to: "exact migrated SHA"
      via: "supported-runner builds and sorted actual chroma_* export diff"
      pattern: "RESULT: identical"
---

<objective>
Obtain and record normal-PR CI evidence for the exact migration commit, and close any pending native-ABI comparison on a supported inspector.

Purpose: Local checks cannot establish D-08's three-platform PR result, and an unavailable local export inspector cannot establish UPG-03; operational access limitations must not be misreported as a failure of the migration itself.
Output: Durable exact-SHA PR CI evidence and a completed native ABI comparison, both bounded to fresh-data binding validation.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-RESEARCH.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-VALIDATION.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-03-SUMMARY.md
</context>

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: Authorize an exact-SHA normal pull request or supply its existing evidence</name>
  <files>No repository file is modified at this checkpoint; Task 2 writes the durable CI evidence summary.</files>
  <read_first>
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-01-SUMMARY.md through 11-03-SUMMARY.md — ensure all substantive migration, test, documentation, ABI, and local-validation work is committed
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-before-inspection.md and 11-abi-export-comparison.md — determine whether Plan 11-03 already produced `RESULT: identical` or a pending supported-inspector ABI gate, and obtain `Pre-migration SHA`
    - git status --short and git log --oneline -n 12 — verify the migration does not omit uncommitted substantive work
    - git rev-parse HEAD — capture `MIGRATED_SHA`
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md — D-08 exact PR requirement and D-09 boundary
  </read_first>
  <what-built>Plans 11-01 through 11-03 have completed the lockfile migration, private source adaptation, regression, documentation, and local fresh-data validation. The remaining evidence is the normal pull-request workflow for the exact committed migration, plus a native ABI comparison if Plan 11-03 could not inspect exports locally.</what-built>
  <action>Capture `MIGRATED_SHA` only after all substantive Phase 11 source, test, lockfile, documentation, and evidence changes are committed. The executor must not push, create, or edit a remote branch or pull request without a separate explicit user authorization. The user may either authorize those remote operations at execution time or provide an already open, non-draft normal PR URL/number whose head is exactly `MIGRATED_SHA`. If `11-abi-export-comparison.md` is `RESULT: pending supported inspector`, also authorize a supported build runner with `nm -gjU`, `nm -D --defined-only`, `llvm-nm`, or `dumpbin /exports`; it must be able to build the recorded `Pre-migration SHA` and `MIGRATED_SHA`. If GitHub CLI authentication/network access will not be available, the user may instead authorize manual CI evidence collection: a maintainer must provide the PR URL, `OPEN`/non-draft state, exact PR head SHA, exact CI-run head SHA (both equal to `MIGRATED_SHA`), CI run URL/ID, successful run conclusion, UTC retrieval time, and the complete job table from that run (every actual job name, status, conclusion). The executor must treat missing remote access as an operational condition requiring this authorized path, not as a failed Rust migration.</action>
  <verify>
    <automated>test -n "$(git rev-parse HEAD)" &amp;&amp; git diff --check</automated>
  </verify>
  <acceptance_criteria>The resume signal supplies `MIGRATED_SHA` plus either an open, non-draft normal PR reference for that exact SHA or explicit authorization for an equivalent manual-evidence record. When the ABI result is pending, it also authorizes a supported inspector runner. No unapproved remote action occurs.</acceptance_criteria>
  <done>The executor has a precise commit identity and an authorized route to collect the PR CI result and, when necessary, complete the native ABI comparison.</done>
  <how-to-verify>
    1. Confirm the migration is committed and run `git rev-parse HEAD`.
    2. Push/create or update an open, non-draft normal PR only with your explicit authorization; do not change its head after capturing the SHA.
    3. If the ABI comparison is pending, identify a supported runner that can build both recorded SHAs and inspect PE/Mach-O/ELF exports.
    4. Resume with `PR ready: &lt;URL-or-number&gt; head &lt;MIGRATED_SHA&gt;`, or authorize the manual-evidence route with the same identity details and supported-runner authorization when applicable.
  </how-to-verify>
  <resume-signal>Reply `PR ready: &lt;PR URL or number&gt; head &lt;MIGRATED_SHA&gt;` or `manual CI evidence authorized: &lt;PR URL or number&gt; head &lt;MIGRATED_SHA&gt;`; if ABI is pending, append `supported inspector authorized: &lt;runner/tool&gt;`.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Collect dynamic CI metadata within 30 minutes or record authorized manual evidence</name>
  <files>.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-04-SUMMARY.md</files>
  <read_first>
    - Task 1 resume signal — PR reference, `MIGRATED_SHA`, and any manual-evidence authorization
    - .github/workflows/ci.yml — `CI` pull-request trigger and the build/test/lint, JNA, and Panama matrices; use as expected coverage context, not a hard-coded job-name list
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-VALIDATION.md — CI polling limit and manual-evidence contract
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md — D-08/D-09 scope
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-04-SUMMARY.md (if present) — preserve prior evidence while appending the current result
  </read_first>
  <action>First require `git rev-parse HEAD` to equal `MIGRATED_SHA`. Close the ABI gate before declaring UPG-03 complete: if `11-abi-export-comparison.md` already says `RESULT: identical`, retain its paths, SHA-256 hashes, inspector commands, and result; if it says `RESULT: pending supported inspector`, use the recorded `Pre-migration SHA` and `MIGRATED_SHA` on the authorized supported runner. In isolated worktrees or equivalent clean checkouts, build each exact SHA, inspect the actual library with the matching supported command (`nm -gjU`, `nm -D --defined-only`, `llvm-nm`, or `dumpbin /exports`), normalize/sort only `chroma_*` exports, update the before/after evidence lists, and require `diff -u` to produce zero output. Update `11-abi-export-comparison.md` with both SHAs, runner OS, paths, commands, list hashes, and `RESULT: identical`; an unavailable inspector, unavailable build, or non-empty diff is a blocking ABI-gate failure, not a passing result. Then preflight `gh auth status`; if it succeeds and network queries work, use `gh pr view` to require the supplied PR is `OPEN`, non-draft, and has `headRefOid == MIGRATED_SHA`. Discover candidates with `gh run list --commit "$MIGRATED_SHA" --event pull_request --limit 100 --json databaseId,headSha,status,conclusion,workflowName,url`, select the normal `CI` workflow candidate whose `headSha` equals `MIGRATED_SHA`, then inspect it with `gh run view "$RUN_ID" --json status,conclusion,headSha,jobs,url`. Poll status at most once every 30 seconds for no more than 30 minutes (60 attempts), re-querying the same JSON fields. On completion require run status `completed`, run conclusion `success`, and require every job returned by the selected run to have status `completed` and conclusion `success`; record every returned job name/status/conclusion exactly as returned. Do not hard-code the nine historical display names or use `gh pr checks --watch`; the completed CI run's JSON is the source of truth. Write `## PR CI evidence` to `11-04-SUMMARY.md` with PR URL/number, `PR state: OPEN`, `Draft: false`, `Local SHA: MIGRATED_SHA`, `PR head SHA: MIGRATED_SHA`, `CI run head SHA: MIGRATED_SHA`, run ID/URL, `CI run status: completed`, `CI run conclusion: success`, UTC timestamp, retrieval method `gh`, poll count, and the complete dynamic job table in exactly `| Job | Status | Conclusion |` form. If `gh auth status`, CLI availability, or network access is unavailable, use Task 1's explicit manual authorization: validate locally available PR/SHA facts, write retrieval method `authorized manual evidence`, preserve the maintainer-supplied open/non-draft PR, exact PR head and CI-run head SHA fields, run/timestamp/conclusion, and every returned job row, and record the exact unavailable preflight output. In either path, do not claim persisted 1.5.5-data upgrade, mutation/reopen, maintenance, or Phase 12 binding compatibility; record this only as fresh-data PR CI evidence. If the ABI comparison fails, a reachable CI run fails, remains incomplete after 30 minutes, has a mismatched SHA, or has a failed/cancelled/skipped job, stop without marking this plan complete; this is a gate failure to fix/rerun. Addresses review concerns: supported-runner ABI closure, authenticated preflight, dynamically discovered job metadata, bounded polling, and authorized manual fallback.</action>
  <verify>
    <automated>SUMMARY=.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-04-SUMMARY.md &amp;&amp; ABI=.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-export-comparison.md &amp;&amp; LOCAL_SHA="$(git rev-parse HEAD)" &amp;&amp; test -s "$SUMMARY" &amp;&amp; rg -q '^RESULT: identical$' "$ABI" &amp;&amp; rg -q '^## PR CI evidence$' "$SUMMARY" &amp;&amp; rg -q '^PR: .+' "$SUMMARY" &amp;&amp; rg -q '^PR state: OPEN$' "$SUMMARY" &amp;&amp; rg -q '^Draft: false$' "$SUMMARY" &amp;&amp; rg -q '^Local SHA: [0-9a-f]{40}$' "$SUMMARY" &amp;&amp; rg -q '^PR head SHA: [0-9a-f]{40}$' "$SUMMARY" &amp;&amp; rg -q '^CI run head SHA: [0-9a-f]{40}$' "$SUMMARY" &amp;&amp; test "$LOCAL_SHA" = "$(sed -n 's/^Local SHA: //p' "$SUMMARY" | tail -n 1)" &amp;&amp; test "$LOCAL_SHA" = "$(sed -n 's/^PR head SHA: //p' "$SUMMARY" | tail -n 1)" &amp;&amp; test "$LOCAL_SHA" = "$(sed -n 's/^CI run head SHA: //p' "$SUMMARY" | tail -n 1)" &amp;&amp; rg -q '^CI run: .+' "$SUMMARY" &amp;&amp; rg -q '^CI run status: completed$' "$SUMMARY" &amp;&amp; rg -q '^CI run conclusion: success$' "$SUMMARY" &amp;&amp; rg -q '^Retrieved at: [0-9]{4}-[0-9]{2}-[0-9]{2}T' "$SUMMARY" &amp;&amp; rg -q '^Retrieval method: (gh|authorized manual evidence)$' "$SUMMARY" &amp;&amp; awk -F'|' '/^### Jobs$/ {jobs=1; next} /^##/ {jobs=0} jobs &amp;&amp; /^\|/ &amp;&amp; $2 !~ /^[[:space:]]*(Job|-+)/ {rows++; if ($3 !~ /^[[:space:]]*completed[[:space:]]*$/ || $4 !~ /^[[:space:]]*success[[:space:]]*$/) bad=1} END {exit !(rows &gt; 0 &amp;&amp; !bad)}' "$SUMMARY"</automated>
  </verify>
  <acceptance_criteria>The actual native before/after `chroma_*` export lists are proven byte-identical with `RESULT: identical`. The summary binds an exact local SHA to an open, non-draft normal PR and completed successful CI run, using live `gh` metadata when possible or explicit authorized manual evidence when it is not. It records PR state, run conclusion, timestamp, and every actual returned job outcome, with no hard-coded job-name assumptions; a failed/cancelled/skipped/incomplete job cannot pass. A reachable failed/incomplete/mismatched run is not accepted, while lack of CLI authentication/network is recorded as an operational limitation with the authorized evidence route.</acceptance_criteria>
  <done>Durable exact-SHA fresh-data PR CI evidence and a completed native ABI comparison exist without claiming Phase 12 persisted-data compatibility or conflating environment access with technical migration health.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local commit to normal PR head | The remote PR must represent exactly the reviewed migration revision. |
| CI run metadata to phase-completion claim | Event/run/job identity must be checked before treating a result as evidence. |
| Fresh-data PR matrix to compatibility statement | CI does not establish existing-data upgrade or maintenance behavior. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-09 | Repudiation | Phase 11 compatibility claim | mitigate | Record SHA, open/non-draft normal PR, run URL/ID, successful run conclusion, retrieval time, and complete actual job table; retain D-09 Phase 12 boundary. |
| T-11-10 | Spoofing | PR/run identity | mitigate | Require local HEAD, PR `headRefOid`, and run `headSha` equality when live metadata is available; manual record preserves the same identifiers. |
| T-11-11 | Tampering | Remote operations | mitigate | Human authorization gate before push/PR changes; executor does not infer remote authority. |
| T-11-12 | Denial of service | CLI/network or export-inspector evidence collection | mitigate | `gh auth status` preflight, 30-minute bounded polling, authorized manual CI evidence, and mandatory supported-runner export comparison; only a successful CI record plus identical actual exports closes UPG-03. |
| T-11-SC | Tampering | Migrated lockfile | mitigate | CI evidence is tied to Plan 11-01's committed exact SHA and locked graph, not an unrelated workflow run. |
</threat_model>

<verification>
Use `gh run view --json status,conclusion,headSha,jobs,url` as the live evidence source when authenticated. The manual branch is valid only with Task 1 authorization and a complete maintainer-provided open-PR/successful-run/job record. If Plan 11-03 could not inspect native exports, the supported-runner before/after comparison is mandatory and must end `RESULT: identical`. Neither path can turn a failed/mismatched CI run or unavailable/different export set into success.
</verification>

<success_criteria>
- Actual native exports are proven identical before/after; inspection unavailability cannot close UPG-03.
- Remote evidence is tied to the exact committed migration SHA and open, non-draft normal PR with completed/successful run conclusion.
- Job evidence is dynamically discovered from actual CI metadata, the complete returned job set is persisted, and every returned job is successful.
- Polling is bounded to 30 minutes; missing remote access is precisely recorded and handled through authorized manual evidence.
- The final CI record is explicitly fresh-data-only; Phase 12 remains the persisted-data release gate.
</success_criteria>

<output>
Create `.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-04-SUMMARY.md` when done.
</output>
