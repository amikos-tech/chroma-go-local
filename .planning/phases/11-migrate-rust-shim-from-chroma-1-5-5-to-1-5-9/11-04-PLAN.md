---
phase: 11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9
plan: "04"
type: execute
wave: 4
depends_on:
  - 11-03
files_modified:
  - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-04-SUMMARY.md
autonomous: false
requirements:
  - UPG-03
user_setup:
  - service: "Git remote pull request"
    why: "D-08 requires a real normal PR CI result for the exact migrated commit; the user must explicitly authorize the remote push and PR creation or update."
    dashboard_config:
      - task: "Push the exact committed Phase 11 branch and create or update an open, non-draft normal pull request at that same head SHA."
        location: "User-controlled Git host CLI or web UI"
must_haves:
  truths:
    - "The exact committed Phase 11 migration is pushed to an open normal PR only after local validation and substantive commits complete (D-08)."
    - "The pull-request-triggered CI run for that exact SHA completes successfully for Build/Test/Lint, Java JNA Smoke, and Java Panama Smoke on Linux, macOS, and Windows (D-08)."
    - "The recorded CI evidence is bounded to fresh-data and cross-platform binding validation, not persisted 1.5.5-data upgrade compatibility (D-08, D-09)."
  artifacts:
    - path: .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-04-SUMMARY.md
      provides: "Recorded normal PR CI evidence: PR URL, exact migrated head SHA, CI run URL, UTC check time, and all nine required job conclusions."
      contains: "PR CI evidence"
  key_links:
    - from: "exact committed Phase 11 head SHA"
      to: "normal pull-request CI workflow"
      via: "open PR headRefOid and CI run headSha equality checks"
      pattern: "headRefOid.*headSha"
    - from: "normal pull-request CI workflow"
      to: .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-04-SUMMARY.md
      via: "recorded successful matrix-job results"
      pattern: "completed / success"
---

<objective>
Obtain user-authorized normal PR evidence for the exact committed migration, then block completion until the required cross-platform CI matrix is green and recorded.

Purpose: Local commands and workflow-source review cannot prove D-08; the phase needs the actual pull-request-triggered result for its migrated commit.
Output: A durable phase summary with the matching PR, SHA, CI run, timestamp, and nine successful job conclusions.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-RESEARCH.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-PATTERNS.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-03-SUMMARY.md
</context>

## Source Coverage Audit Continuation

The complete GOAL/REQ/RESEARCH/CONTEXT audit is maintained in `11-03-PLAN.md` and explicitly maps D-08's final normal-PR evidence to this Plan 11-04. This plan supplies that previously missing execution route without changing Phase 11 scope.

<tasks>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 1: User-authorize the exact branch push and normal PR</name>
  <files>No repository file is modified at this checkpoint; Task 2 creates the phase summary after the user resumes.</files>
  <read_first>
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-01-SUMMARY.md through 11-03-SUMMARY.md — confirm the dependency migration, FFI adaptation, documentation, and local validation tasks have completed
    - git log --oneline -n 12 and git status --short — identify the committed migration state and ensure no uncommitted substantive Phase 11 source, test, or documentation change is omitted from it
    - git rev-parse HEAD — capture the precise migrated SHA before any remote action
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md — D-08 requires a green normal PR matrix for this exact migrated commit; D-09 bounds its claim
  </read_first>
  <what-built>Plans 11-01 through 11-03 have produced and committed the dependency migration, internal FFI adaptation, regression test, contributor guidance, and local fresh-data validation evidence. The remote PR action is intentionally outside agent authority.</what-built>
  <action>The executor must pause here after confirming every substantive Phase 11 source, lockfile, test, and documentation change is committed and capturing `MIGRATED_SHA` with `git rev-parse HEAD`. The executor must not run `git push`, `gh pr create`, `gh pr edit`, or any other command that creates or updates a remote branch or pull request. The user must explicitly push that exact committed branch and create or update one open, non-draft normal pull request whose head points to `MIGRATED_SHA`; do not amend, rebase, or add a substantive commit after capturing the SHA. A later automated task will reject any different SHA, absent PR, draft PR, or non-PR CI result.</action>
  <acceptance_criteria>The user supplies an open normal PR URL/number and confirms its `headRefOid` is exactly the locally captured `MIGRATED_SHA`; all substantive Phase 11 migration changes are already committed locally. No agent-initiated push or PR creation occurs.</acceptance_criteria>
  <how-to-verify>
    1. Record `MIGRATED_SHA` from `git rev-parse HEAD` after verifying the Phase 11 source, lockfile, test, and documentation changes are committed.
    2. Using your own authorized Git remote access, push that exact branch and create or update an open, non-draft normal pull request without changing the captured commit.
    3. Confirm in the PR UI or with your own tooling that its head SHA equals `MIGRATED_SHA`.
    4. Provide the PR URL or number and the exact SHA to resume automated CI verification.
  </how-to-verify>
  <verify>
    <automated>test -n "$(git rev-parse HEAD)" &amp;&amp; git diff --check</automated>
  </verify>
  <done>The executor has stopped after confirming a concrete migrated commit exists; only the user can advance the plan by supplying an exact-SHA normal PR.</done>
  <resume-signal>Reply `PR ready: &lt;PR URL or number&gt; head &lt;MIGRATED_SHA&gt;` after the open normal PR points at the exact captured commit.</resume-signal>
</task>

<task type="auto">
  <name>Task 2: Verify and record the SHA-matched normal PR CI matrix</name>
  <files>.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-04-SUMMARY.md</files>
  <read_first>
    - the Task 1 resume signal — supplied normal PR URL or number and the precise migrated SHA to validate
    - .github/workflows/ci.yml:1-40 — the `CI` pull-request workflow and its Build/Test/Lint Linux/macOS/Windows matrix
    - .github/workflows/ci.yml:152-161 and 247-268 — the Java JNA Smoke matrix and native-shim smoke-test command
    - .github/workflows/ci.yml:270-279 and 365-403 — the Java Panama Smoke matrix and native-shim smoke-test command
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md — D-08 exact-PR requirement and D-09 persisted-data boundary
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-04-SUMMARY.md (if present) — preserve earlier summary content before appending CI evidence
  </read_first>
  <action>After Task 1 receives a valid `PR ready` resume signal, set `MIGRATED_SHA` to its supplied SHA and first require `git rev-parse HEAD` to equal it. Use `gh pr view` on the supplied PR to require `state == OPEN`, `isDraft == false`, and `headRefOid == MIGRATED_SHA`; do not substitute a push result, release result, workflow-dispatch result, or workflow-source review. Use `gh pr checks "$PR_NUMBER" --watch --fail-fast --interval 10` to wait for checks to settle, then use `gh run list --workflow CI --commit "$MIGRATED_SHA" --event pull_request` and `gh run view "$RUN_ID" --json status,conclusion,headSha,url,jobs` to find the pull-request-triggered `CI` workflow run for that exact SHA. Require one completed/successful run and each of the nine named jobs — `Build/Test/Lint`, `Java JNA Smoke`, and `Java Panama Smoke` on `ubuntu-latest`, `macos-latest`, and `windows-latest` — to have `completed`/`success` conclusions. Create or append a `## PR CI evidence` section in `11-04-SUMMARY.md` containing exactly `PR: &lt;number&gt; &lt;URL&gt;`, `Head SHA: &lt;SHA&gt;`, `CI run: &lt;ID&gt; &lt;URL&gt;`, `Checked at: &lt;UTC ISO-8601 timestamp&gt;`, then one `- &lt;job name&gt;: completed / success` line for every required job. If the PR or run is absent, the SHA differs, any job is queued, skipped, cancelled, timed out, or unsuccessful, or the summary cannot be recorded, stop without completing Plan 11-04 or Phase 11; fix or rerun CI, then repeat this task. Per D-09, record the result only as fresh-data and cross-platform binding evidence, never as persisted 1.5.5-data upgrade, mutation/reopen, or maintenance compatibility proof. This task must not push a branch or create/update a PR.</action>
  <verify>
    <automated>SUMMARY=.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-04-SUMMARY.md &amp;&amp; MIGRATED_SHA="$(sed -n 's/^Head SHA: //p' "$SUMMARY" | tail -n 1)" &amp;&amp; PR_NUMBER="$(sed -n 's/^PR: \([0-9][0-9]*\) .*/\1/p' "$SUMMARY" | tail -n 1)" &amp;&amp; test -n "$MIGRATED_SHA" &amp;&amp; test -n "$PR_NUMBER" &amp;&amp; test "$(git rev-parse HEAD)" = "$MIGRATED_SHA" &amp;&amp; gh pr view "$PR_NUMBER" --json state,isDraft,headRefOid --jq 'select(.state == "OPEN" and .isDraft == false and .headRefOid == "'"$MIGRATED_SHA"'")' &amp;&amp; gh pr checks "$PR_NUMBER" --watch --fail-fast --interval 10 &amp;&amp; RUN_ID="$(gh run list --workflow CI --commit "$MIGRATED_SHA" --event pull_request --limit 20 --json databaseId,headSha,workflowName --jq '[.[] | select(.workflowName == "CI" and .headSha == "'"$MIGRATED_SHA"'")] | if length == 1 then .[0].databaseId else empty end')" &amp;&amp; test -n "$RUN_ID" &amp;&amp; gh run view "$RUN_ID" --exit-status &amp;&amp; gh run view "$RUN_ID" --json status,conclusion,headSha,jobs --jq 'select(.status == "completed" and .conclusion == "success" and .headSha == "'"$MIGRATED_SHA"'") | [.jobs[] | select((.name == "Build/Test/Lint (ubuntu-latest)") or (.name == "Build/Test/Lint (macos-latest)") or (.name == "Build/Test/Lint (windows-latest)") or (.name == "Java JNA Smoke (ubuntu-latest)") or (.name == "Java JNA Smoke (macos-latest)") or (.name == "Java JNA Smoke (windows-latest)") or (.name == "Java Panama Smoke (ubuntu-latest)") or (.name == "Java Panama Smoke (macos-latest)") or (.name == "Java Panama Smoke (windows-latest)"))] | (length == 9 and all(.[]; .status == "completed" and .conclusion == "success"))' | rg -x 'true' &amp;&amp; rg -q '^PR: [0-9]+ https://' "$SUMMARY" &amp;&amp; rg -q '^Head SHA: '"$MIGRATED_SHA"'$' "$SUMMARY" &amp;&amp; rg -q '^CI run: [0-9]+ https://' "$SUMMARY" &amp;&amp; rg -q '^Checked at: [0-9]{4}-[0-9]{2}-[0-9]{2}T' "$SUMMARY" &amp;&amp; for JOB in 'Build/Test/Lint (ubuntu-latest)' 'Build/Test/Lint (macos-latest)' 'Build/Test/Lint (windows-latest)' 'Java JNA Smoke (ubuntu-latest)' 'Java JNA Smoke (macos-latest)' 'Java JNA Smoke (windows-latest)' 'Java Panama Smoke (ubuntu-latest)' 'Java Panama Smoke (macos-latest)' 'Java Panama Smoke (windows-latest)'; do rg -qF -- "- $JOB: completed / success" "$SUMMARY" || exit 1; done</automated>
  </verify>
  <acceptance_criteria>An open, non-draft PR for the exact committed `MIGRATED_SHA` has one completed/successful `CI` workflow run triggered by `pull_request`; all nine required Build/Test/Lint, Java JNA Smoke, and Java Panama Smoke Linux/macOS/Windows jobs report `completed`/`success`; and `11-04-SUMMARY.md` records the matching PR, SHA, run URL, UTC timestamp, and every job result.</acceptance_criteria>
  <done>The actual normal PR CI matrix is verifiably green for the precise migrated commit, durable evidence exists in the phase summary, no persisted-data compatibility claim has been made, and this plan fails closed whenever D-08 evidence is unavailable or unsuccessful.</done>
</task>

</tasks>

<threat_model>
## ASVS-L1 Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local committed migration to remote PR | The reviewed local commit becomes a remote PR head and must not be replaced by a different revision. |
| Pull-request CI run to phase completion claim | Remote status is evidence only when the CI event, SHA, run conclusion, and required job conclusions all match. |
| Fresh-data validation to compatibility claim | A successful fresh-data matrix is not proof that existing 1.5.5 data can be upgraded safely. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | ASVS-L1 mitigation plan |
|-----------|----------|-----------|-------------|--------------------------|
| T-11-09 | Repudiation | Phase 11 compatibility claim | mitigate | Require a user-authorized normal PR and one SHA-matched pull-request CI run; record the PR, SHA, run URL, UTC timestamp, and every required job conclusion. |
| T-11-10 | Spoofing | PR and CI identity | mitigate | Check the open, non-draft PR's `headRefOid`, the local committed SHA, and the CI run's `headSha` for exact equality; reject other event types and run sources. |
| T-11-11 | Tampering | Remote branch and PR creation | mitigate | Stop at a blocking human-action checkpoint; the executor never pushes or creates/updates a PR, and automated verification begins only after the user's explicit resume signal. |
| T-11-SC | Tampering | Migrated dependency build inputs | mitigate | Preserve the committed `--locked` graph proven in Plans 11-01 and 11-02; this plan installs no packages and accepts no unverified run from another commit. |
</threat_model>

<verification>
Task 1 is a blocking human-action checkpoint. After the user pushes the exact committed branch and creates or updates the normal PR, Task 2 must find one pull-request-triggered `CI` run for that exact SHA, require all nine Linux/macOS/Windows matrix jobs to be completed/successful, and write durable evidence to `11-04-SUMMARY.md`. Do not claim success when the PR, run, or required jobs are absent or unsuccessful.
</verification>

<success_criteria>
- The user, not the executor, explicitly pushes the exact committed Phase 11 branch and creates or updates its open normal PR.
- The PR head SHA, local migrated SHA, and `CI` run head SHA are identical.
- The completed successful `CI` run reports success for all nine Build/Test/Lint, Java JNA Smoke, and Java Panama Smoke Linux/macOS/Windows jobs.
- `11-04-SUMMARY.md` retains the exact PR, SHA, run, timestamp, and job-level proof while reserving persisted-data compatibility for Phase 12.
</success_criteria>

<output>
Create `.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-04-SUMMARY.md` when done.
</output>
