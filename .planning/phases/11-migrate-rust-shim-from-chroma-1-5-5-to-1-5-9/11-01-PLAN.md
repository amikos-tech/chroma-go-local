---
phase: 11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - shim/Cargo.toml
  - shim/Cargo.lock
  - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-before.exports
  - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-before-inspection.md
autonomous: true
requirements:
  - UPG-01
  - UPG-04
must_haves:
  truths:
    - "All nine direct Chroma packages resolve from the official tag-1.5.9 source in locked Cargo metadata (D-05)."
    - "The committed graph is constrained to fastrace 0.7.8 and accepts the Rust 1.88.0 locked all-targets check (D-05, D-06, D-07)."
    - "A real pre-migration native chroma_* export baseline, or a precise inspector-unavailability record, exists before the dependency edit."
  artifacts:
    - path: shim/Cargo.toml
      provides: "Nine synchronized official Chroma 1.5.9 git pins and the Rust 1.88 source-build MSRV declaration."
      contains: "tag = \"1.5.9\""
    - path: shim/Cargo.lock
      provides: "Generated, reproducible Chroma 1.5.9 resolution with fastrace 0.7.8."
      contains: "name = \"fastrace\""
    - path: .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-before.exports
      provides: "Sorted actual pre-migration chroma_* export list when an inspector is available."
    - path: .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-before-inspection.md
      provides: "Platform, library path, inspector command, and exact unavailable/error record when applicable."
  key_links:
    - from: shim/Cargo.toml
      to: shim/Cargo.lock
      via: "package-specific cargo update"
      pattern: "chroma-core/chroma.git\\?tag=1.5.9"
    - from: "cargo metadata --locked JSON"
      to: "nine expected Chroma package names"
      via: "jq structural predicate"
      pattern: "chroma-(config|frontend|index|log|segment|sqlite|sysdb|system|types)"
---

<objective>
Capture the current native ABI evidence, migrate the nine direct Chroma pins as one constrained Cargo resolution, and prove the resulting locked graph semantically.

Purpose: Dependency drift is the principal migration risk; a real pre-change export baseline also enables an artifact-level ABI comparison after rebuilding.
Output: Updated manifest and generated lockfile, plus durable pre-migration native-export evidence.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-RESEARCH.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-VALIDATION.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-PATTERNS.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Capture the actual pre-migration chroma_* native export set</name>
  <files>.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-before.exports, .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-before-inspection.md</files>
  <read_first>
    - Makefile — `make build`, platform library-name rules, and debug-shim output location
    - shim/Cargo.toml — current cdylib package name and existing 1.5.5 inputs
    - shim/src/lib.rs — existing `#[no_mangle]` C exports; this is context only, not a source-only ABI proof
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-VALIDATION.md — Wave 0 native-export baseline requirement
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md — D-02 and D-05 scope constraints
  </read_first>
  <action>Before changing `shim/Cargo.toml` or `shim/Cargo.lock`, run `make build` and resolve the produced debug library from the repository platform rules: `libchroma_shim.dylib` on macOS, `libchroma_shim.so` on Linux, or `chroma_shim.dll` on Windows. Inspect the actual dynamic library, never source annotations: on macOS use `nm -gjU`; on Linux use `nm -D --defined-only`; on Windows probe `llvm-nm` first and then `dumpbin /exports`. Normalize only the observed exported `chroma_*` names, sort uniquely, and write them to `11-abi-before.exports`. Write `11-abi-before-inspection.md` with UTC timestamp, OS, resolved library path, exact inspector command, command exit status, and list SHA-256. If no supported inspector is available or the build/library cannot be inspected, write `UNAVAILABLE` plus the exact attempted commands, PATH/tool discovery output, library path, and error; do not fabricate a list, do not silently skip, and preserve this record for Plan 11-03. Addresses review concern: explicit actual native ABI baseline rather than source-only evidence.</action>
  <verify>
    <automated>test -s .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-before-inspection.md &amp;&amp; (test -s .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-before.exports || rg -q '^UNAVAILABLE:' .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-abi-before-inspection.md)</automated>
  </verify>
  <acceptance_criteria>A durable inspection record identifies the built library and command used. When an inspector is available, the baseline is non-empty, sorted, contains only `chroma_*` names, and is captured before the manifest change; otherwise the record precisely explains why no comparison can run on that platform.</acceptance_criteria>
  <done>Plan 11-03 has valid pre-migration artifact evidence to compare, or an explicit unavailable-state to report without misrepresenting ABI coverage.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Pin the official Chroma 1.5.9 graph and verify it structurally</name>
  <files>shim/Cargo.toml, shim/Cargo.lock</files>
  <read_first>
    - shim/Cargo.toml — package block and all nine current `chroma-*` direct git dependencies
    - shim/Cargo.lock — generated lockfile; never hand-edit it
    - .planning/spikes/001-chroma-1-5-9-lock-resolution/README.md — validated tag-update and fastrace reconciliation procedure
    - .planning/spikes/002-chroma-1-5-9-toolchain-floor/README.md — Rust 1.88.0 source-build evidence
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md — D-05 through D-07 are locked
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-RESEARCH.md — nine-package list and semantic Cargo metadata predicate rationale
  </read_first>
  <behavior>
    - The expected package-name set is exactly `chroma-config`, `chroma-frontend`, `chroma-index`, `chroma-log`, `chroma-segment`, `chroma-sqlite`, `chroma-sysdb`, `chroma-system`, and `chroma-types`.
    - Locked Cargo metadata resolves every expected package from `git+https://github.com/chroma-core/chroma.git?tag=1.5.9#...`, resolves no expected package from tag 1.5.5, and contains `fastrace` 0.7.8.
    - Rust 1.88.0 accepts the final graph without changing `shim/Cargo.lock`.
  </behavior>
  <action>Per D-05, change all nine existing direct Chroma dependency `tag` values together from `1.5.5` to `1.5.9`; do not add any deferred Chroma capability or dependency. Add `rust-version = "1.88"` to the existing `shim/Cargo.toml` package block as the source-build MSRV per D-06. Generate the lockfile only through `cargo update --manifest-path shim/Cargo.toml -p fastrace --precise 0.7.8`; never run bare `cargo update` and never hand-edit `Cargo.lock`. Review `git diff -- shim/Cargo.toml shim/Cargo.lock` and `cargo tree --manifest-path shim/Cargo.toml --duplicates` for unrelated churn. Then run locked metadata and evaluate this concrete JSON predicate with `jq -e`, using the literal expected-name array: collect packages whose name is in that array and whose source starts with `git+https://github.com/chroma-core/chroma.git?tag=1.5.9#`; require its sorted unique names to equal the expected sorted array, require no expected package source to contain `tag=1.5.5`, and require a package named `fastrace` at version `0.7.8`. Run `cargo +1.88.0 check --manifest-path shim/Cargo.toml --all-targets --locked` after the lockfile is final. Addresses review concerns: use Cargo's locked JSON resolution, not formatting-sensitive `awk` or exact text counts, and prove every one of the nine sources.</action>
  <verify>
    <automated>cargo metadata --manifest-path shim/Cargo.toml --locked --format-version=1 | jq -e --argjson expected '["chroma-config","chroma-frontend","chroma-index","chroma-log","chroma-segment","chroma-sqlite","chroma-sysdb","chroma-system","chroma-types"]' '([.packages[] | select(.name as $n | $expected | index($n)) | select(.source? != null and (.source | startswith("git+https://github.com/chroma-core/chroma.git?tag=1.5.9#"))) | .name] | unique | sort) as $resolved | ($resolved == ($expected | sort)) and ([.packages[] | select(.name as $n | $expected | index($n)) | select(.source? != null and (.source | contains("chroma-core/chroma.git?tag=1.5.5"))) | .name] | length == 0) and ([.packages[] | select(.name == "fastrace" and .version == "0.7.8")] | length &gt;= 1)' &amp;&amp; cargo +1.88.0 check --manifest-path shim/Cargo.toml --all-targets --locked</automated>
  </verify>
  <acceptance_criteria>All nine direct manifest pins use tag 1.5.9; the committed generated lockfile passes the stated metadata predicate and Rust 1.88.0 locked check; fastrace is 0.7.8; and the diff/duplicate review contains no unexplained unconstrained resolver drift.</acceptance_criteria>
  <done>The dependency graph is reproducibly migrated and semantically proven before private source adaptation begins.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Manifest and Cargo resolver to committed lockfile | Official upstream inputs become locally trusted build inputs. |
| Existing dynamic library to recorded ABI baseline | A compiled artifact, not Rust source, defines what Go and Java consumers can load. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-11-01 | Tampering | Chroma dependency graph | mitigate | D-05 constrained `fastrace` update, nine-name locked metadata predicate, duplicate review, and `--locked` Rust 1.88.0 check. |
| T-11-02 | Tampering | Native `chroma_*` ABI | mitigate | Capture actual pre-migration library exports and exact inspection metadata before source/dependency edits. |
| T-11-04 | Denial of service | Upstream graph/toolchain compatibility | mitigate | Declare Rust 1.88 MSRV and compile the final locked graph immediately. |
| T-11-SC | Tampering | Cargo inputs | mitigate | No new package install; use only official existing Chroma git dependencies and record generated-lockfile review. |
</threat_model>

<verification>
The plan succeeds only when both the pre-change artifact evidence and the locked Cargo metadata predicate exist. A source grep or an absent old tag is not a substitute for the all-nine resolver assertion.
</verification>

<success_criteria>
- The nine expected Chroma package names resolve from official tag 1.5.9 source in `cargo metadata --locked`.
- The graph contains fastrace 0.7.8 and compiles with Rust 1.88.0 without lock mutation.
- A before-migration actual export list or precisely documented inspector unavailability is retained for later comparison.
</success_criteria>

<output>
Create `.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-01-SUMMARY.md` when done.
</output>
