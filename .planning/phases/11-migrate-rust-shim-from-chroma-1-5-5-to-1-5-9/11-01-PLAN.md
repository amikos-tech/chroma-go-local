---
phase: 11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - shim/Cargo.toml
  - shim/Cargo.lock
autonomous: true
requirements:
  - UPG-01
user_setup: []
must_haves:
  truths:
    - "The shim's nine direct Chroma dependencies resolve from the official 1.5.9 tag as one graph (D-05)."
    - "The committed Cargo lockfile contains the targeted fastrace 0.7.8 reconciliation and is not refreshed by an unconstrained update (D-05); the manifest records the hybrid source-build toolchain floor (D-06)."
  artifacts:
    - path: shim/Cargo.toml
      provides: "Nine synchronized Chroma 1.5.9 git tags and an explicit Rust 1.88 MSRV declaration."
      contains: 'rust-version = "1.88"'
    - path: shim/Cargo.lock
      provides: "Reproducible resolution for the migrated Chroma graph."
      contains: 'name = "fastrace"'
  key_links:
    - from: shim/Cargo.toml
      to: shim/Cargo.lock
      via: "targeted cargo update for fastrace"
      pattern: "fastrace.*0\\.7\\.8"
---

<objective>
Move the shim's dependency graph from Chroma 1.5.5 to 1.5.9 without accepting unrelated resolver drift.

Purpose: Produce a reviewable, locked Rust dependency migration that downstream source adaptation can compile against.
Output: Updated `shim/Cargo.toml` and generated `shim/Cargo.lock` with all nine Chroma pins synchronized at 1.5.9.
</objective>

<execution_context>
@/Users/tazarov/.codex/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.codex/get-shit-done/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-RESEARCH.md
@.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-PATTERNS.md
@.planning/spikes/001-chroma-1-5-9-lock-resolution/README.md
@.planning/spikes/002-chroma-1-5-9-toolchain-floor/README.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Pin the complete direct Chroma dependency group and record the measured MSRV</name>
  <files>shim/Cargo.toml</files>
  <read_first>
    - shim/Cargo.toml:1-45 — package metadata and the complete nine-crate Chroma dependency group
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-CONTEXT.md — D-05, D-06, and D-07 are locked
    - .planning/spikes/001-chroma-1-5-9-lock-resolution/README.md — validated resolver procedure and bare-update prohibition
    - .planning/spikes/002-chroma-1-5-9-toolchain-floor/README.md — measured 1.88.0 source-build floor
  </read_first>
  <action>
    Per D-05, replace the `tag = "1.5.5"` value in every one of the nine existing direct Chroma dependency declarations — `chroma-frontend`, `chroma-config`, `chroma-system`, `chroma-types`, `chroma-log`, `chroma-index`, `chroma-segment`, `chroma-sysdb`, and `chroma-sqlite` — with `tag = "1.5.9"` in one manifest edit. Keep the dependency names, git URL, dependency features, crate types, and all non-Chroma dependencies unchanged. Per D-06 and the validated MSRV probe, add `rust-version = "1.88"` to the package metadata after `edition = "2021"`; this expresses the measured source-build floor without changing the existing Rust 1.93.1 CI/release pin. Do not add any of Chroma 1.5.9's optional/additive crates or APIs.
  </action>
  <verify>
    <automated>awk '/^chroma-(frontend|config|system|types|log|index|segment|sysdb|sqlite)[[:space:]]*=/{if ($0 ~ /tag = "1.5.9"/) ok++; else bad++} END {exit !(ok == 9 &amp;&amp; bad == 0)}' shim/Cargo.toml &amp;&amp; rg -n '^rust-version = "1\.88"$' shim/Cargo.toml</automated>
  </verify>
  <acceptance_criteria>Exactly nine direct Chroma declarations use tag 1.5.9, none use 1.5.5, and the package metadata declares Rust 1.88 as the source-build MSRV.</acceptance_criteria>
  <done>The manifest contains one coherent 1.5.9 Chroma dependency group and no unrequested dependency or public-interface additions.</done>
</task>

<task type="auto">
  <name>Task 2: Generate and inspect the constrained 1.5.9 lockfile migration</name>
  <files>shim/Cargo.lock</files>
  <read_first>
    - shim/Cargo.toml — the nine edited 1.5.9 direct dependency pins and Rust MSRV declaration
    - shim/Cargo.lock:1-3 — generated-lockfile boundary; do not hand-edit checksum or source records
    - shim/Cargo.lock — current root dependency and `fastrace` package records
    - .planning/spikes/001-chroma-1-5-9-lock-resolution/README.md — exact `cargo update -p fastrace --precise 0.7.8` command and expected changes
    - .planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-PATTERNS.md — lockfile generation and duplicate-review conventions
  </read_first>
  <action>
    Per D-05, from the repository root run `cargo update --manifest-path shim/Cargo.toml -p fastrace --precise 0.7.8` after the nine manifest pins are changed. Treat the resulting `shim/Cargo.lock` as generated output: do not hand-edit its source hashes, package records, or checksums. Inspect `git diff -- shim/Cargo.toml shim/Cargo.lock` and `cargo tree --manifest-path shim/Cargo.toml --duplicates`; confirm the Chroma git source moved to 1.5.9, the selected `fastrace` package is 0.7.8, and any material duplicate or transitive-package changes are explainable by the Chroma graph. Do not run bare `cargo update`, because the validated spike shows it refreshes unrelated packages and can increase the compiler floor. The source adaptation in Plan 11-02 is intentionally the next dependency before a successful locked all-targets compilation.
  </action>
  <verify>
    <automated>cargo tree --manifest-path shim/Cargo.toml -p fastrace &amp;&amp; awk '/^name = "fastrace"$/{seen=1; next} seen &amp;&amp; /^version = "0\.7\.8"$/{found=1} END {exit !found}' shim/Cargo.lock &amp;&amp; ! rg -n 'git\+https://github\.com/chroma-core/chroma\.git\?tag=1\.5\.5' shim/Cargo.lock &amp;&amp; rg -n 'git\+https://github\.com/chroma-core/chroma\.git\?tag=1\.5\.9' shim/Cargo.lock</automated>
  </verify>
  <acceptance_criteria>The generated lockfile resolves the same nine direct dependencies against Chroma 1.5.9, contains fastrace 0.7.8, has no remaining 1.5.5 Chroma git source, and has been reviewed with the duplicate-package tree.</acceptance_criteria>
  <done>The lockfile is a constrained, explainable dependency migration ready for the Rust source adaptation and subsequent `--locked` validation.</done>
</task>

</tasks>

<threat_model>
## ASVS-L1 Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Cargo manifest to generated lockfile | A declared git dependency becomes the exact source and checksum graph compiled into the native shim. |
| Native shim to language bindings | Rust internals back a stable C ABI consumed by purego, JNA, and Panama. |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | ASVS-L1 mitigation plan |
|-----------|----------|-----------|-------------|--------------------------|
| T-11-01 | Tampering | `shim/Cargo.toml` and `shim/Cargo.lock` | mitigate | Pin all nine official Chroma crates to one 1.5.9 tag, use the explicit `fastrace` 0.7.8 resolver command, inspect the generated diff and duplicate tree, and prohibit bare `cargo update`. |
| T-11-02 | Tampering | Rust build inputs | mitigate | Preserve the committed lockfile as the build authority; Plan 11-02 runs `cargo +1.88.0 check --all-targets --locked` after the required source compatibility edit. |
| T-11-SC | Tampering | Cargo package resolution | mitigate | Review git source changes and registry package additions as supply-chain evidence before accepting the lockfile; no new package manager is introduced. |
| T-11-03 | Elevation of privilege | Rust FFI bridge | mitigate | This plan changes no FFI export; Plan 11-02 verifies the upstream call adaptation remains behind existing `extern "C"` signatures. |
</threat_model>

<verification>
Run the two task commands. Before handing off, inspect the Cargo diff and duplicate tree; leave the next plan to perform the locked all-targets check only after it has updated the one incompatible Rust call site.
</verification>

<success_criteria>
- `shim/Cargo.toml` pins all nine direct Chroma crates to 1.5.9 and records Rust 1.88 MSRV.
- `shim/Cargo.lock` is generated through the targeted fastrace 0.7.8 reconciliation, not an unconstrained update.
- The resolved lockfile contains no Chroma 1.5.5 git source records.
</success_criteria>

<output>
Create `.planning/phases/11-migrate-rust-shim-from-chroma-1-5-5-to-1-5-9/11-01-SUMMARY.md` when done.
</output>
