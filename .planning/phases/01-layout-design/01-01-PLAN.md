---
phase: 01-layout-design
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - internal/runtime/runtime.go
  - internal/library/library.go
  - internal_test.go
autonomous: true
requirements:
  - LAYOUT-01
  - LAYOUT-02

must_haves:
  truths:
    - "internal/runtime/ and internal/library/ directories exist at the module root with valid Go package declarations"
    - "go build ./... succeeds with the skeleton packages alongside existing root files"
    - "The root package can import internal/runtime and internal/library without 'use of internal package not allowed' errors"
    - "No existing Go files have been relocated — the skeleton is additive only"
  artifacts:
    - path: "internal/runtime/runtime.go"
      provides: "Skeleton package for server/embedded runtime implementation"
      contains: "package runtime"
    - path: "internal/library/library.go"
      provides: "Skeleton package for platform-specific FFI loading"
      contains: "package library"
    - path: "internal_test.go"
      provides: "Anchor validation test proving root can import internal packages"
      contains: "package chroma_test"
  key_links:
    - from: "internal_test.go"
      to: "internal/runtime"
      via: "blank import"
      pattern: '_ "github.com/amikos-tech/chroma-go-local/internal/runtime"'
    - from: "internal_test.go"
      to: "internal/library"
      via: "blank import"
      pattern: '_ "github.com/amikos-tech/chroma-go-local/internal/library"'
---

<objective>
Create the target directory structure for the Go subtree reorganization by adding two skeleton packages (`internal/runtime/` and `internal/library/`) and an anchor validation test. This is strictly additive -- no existing files are modified or moved.

Purpose: Establish the `internal/` subtree that Phase 2 will migrate implementation files into, and prove at compile-time that the root package can import from it.
Output: Three new files -- two skeleton packages and one anchor test.
</objective>

<execution_context>
@/Users/tazarov/.claude/get-shit-done/workflows/execute-plan.md
@/Users/tazarov/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-layout-design/01-CONTEXT.md
@.planning/phases/01-layout-design/01-RESEARCH.md
@go.mod
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create skeleton packages in internal/runtime/ and internal/library/</name>
  <files>internal/runtime/runtime.go, internal/library/library.go</files>
  <read_first>
    - go.mod (confirm module path is github.com/amikos-tech/chroma-go-local)
    - .planning/phases/01-layout-design/01-CONTEXT.md (locked decisions on package naming and skeleton depth)
  </read_first>
  <action>
Create two directories and one Go file in each:

1. Create `internal/runtime/runtime.go` with exactly this content:
```go
// Package runtime contains the Chroma server and embedded runtime implementation.
package runtime
```

2. Create `internal/library/library.go` with exactly this content:
```go
// Package library provides platform-specific FFI loading for the Chroma shared library.
package library
```

Rules from locked decisions:
- Bare package declarations only -- no stub types, no placeholder functions, no imports
- Doc comment is 1-2 lines explaining the package's intended role
- Doc comment lives in the main .go file (runtime.go, library.go), NOT a separate doc.go
- No .gitkeep files -- the .go files make directories trackable
- No init() functions in skeleton packages
  </action>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/local-go-chroma && go build ./internal/...</automated>
  </verify>
  <acceptance_criteria>
    - internal/runtime/runtime.go exists and contains the string "package runtime"
    - internal/runtime/runtime.go contains the string "// Package runtime"
    - internal/library/library.go exists and contains the string "package library"
    - internal/library/library.go contains the string "// Package library"
    - internal/runtime/runtime.go contains exactly 2 lines (doc comment + package declaration)
    - internal/library/library.go contains exactly 2 lines (doc comment + package declaration)
    - `go build ./internal/...` exits 0
  </acceptance_criteria>
  <done>Both skeleton packages compile successfully with bare package declarations and doc comments. No existing files modified.</done>
</task>

<task type="auto">
  <name>Task 2: Create anchor validation test and verify full build</name>
  <files>internal_test.go</files>
  <read_first>
    - go.mod (confirm module path for import statements)
    - internal/runtime/runtime.go (confirm package exists from Task 1)
    - internal/library/library.go (confirm package exists from Task 1)
    - .planning/phases/01-layout-design/01-CONTEXT.md (locked decision: internal_test.go uses package chroma_test, blank-imports both internal packages, is temporary -- removed after Phase 3)
  </read_first>
  <action>
Create `internal_test.go` at the repo root with exactly this content:
```go
package chroma_test

import (
	_ "github.com/amikos-tech/chroma-go-local/internal/library"
	_ "github.com/amikos-tech/chroma-go-local/internal/runtime"
)
```

This file:
- Uses `package chroma_test` (external test perspective, NOT `package chroma`)
- Blank-imports both internal packages (the `_` import ensures the compiler validates visibility without needing any symbols)
- Proves the root package can import `internal/` without "use of internal package not allowed" errors
- Is temporary -- will be removed in Phase 3 when the real facade exists

After creating the file, run the full verification suite:
1. `go build ./...` -- all packages including skeletons compile
2. `go vet ./...` -- static analysis passes
3. `go test ./...` -- tests pass (internal packages show [no test files], which is informational not an error)
4. `golangci-lint run ./...` -- linter passes (stale gci prefix is a Phase 4 concern, not blocking here)
  </action>
  <verify>
    <automated>cd /Users/tazarov/experiments/amikos/local-go-chroma && go build ./... && go vet ./... && golangci-lint run ./...</automated>
  </verify>
  <acceptance_criteria>
    - internal_test.go exists at repo root (not inside internal/)
    - internal_test.go contains the string "package chroma_test"
    - internal_test.go contains the string `_ "github.com/amikos-tech/chroma-go-local/internal/library"`
    - internal_test.go contains the string `_ "github.com/amikos-tech/chroma-go-local/internal/runtime"`
    - `go build ./...` exits 0
    - `go vet ./...` exits 0
    - `golangci-lint run ./...` exits 0
    - No existing Go files at repo root have been modified (git diff shows only additions)
  </acceptance_criteria>
  <done>Anchor validation test compiles successfully, proving root-to-internal imports work. Full build (go build, go vet, golangci-lint) passes with zero errors. No existing files modified -- phase is purely additive.</done>
</task>

</tasks>

<verification>
Phase 1 gate -- all four commands must pass:
1. `go build ./...` -- compiles all packages including new skeletons
2. `go vet ./...` -- static analysis clean
3. `go test ./...` -- all tests pass (internal packages show [no test files])
4. `golangci-lint run ./...` -- linter clean

Additional checks:
- `git diff --name-only` shows only 3 new files: internal/runtime/runtime.go, internal/library/library.go, internal_test.go
- `git diff --name-only --diff-filter=M` shows no modified files (purely additive)
</verification>

<success_criteria>
- internal/runtime/ and internal/library/ directories exist at module root with valid package declarations
- go build ./... succeeds with skeletons alongside existing root files
- internal_test.go at repo root proves root package can import both internal packages
- No existing Go files relocated or modified
- golangci-lint run ./... reports zero issues
</success_criteria>

<output>
After completion, create `.planning/phases/01-layout-design/01-01-SUMMARY.md`
</output>
